# 廃棄物実測値管理機能 開発計画（廃棄物）

## 1. 目的と範囲

### 目的

現在、BKBビル（備後町コイズミビル）の一般廃棄物の重量計測は次の運用で行っている。

1. 添付PDF（`廃棄物月別集計表（V3）2026年度.pdf`）を1ヶ月分の紙シートとして印刷し、スタッフへ渡す
2. スタッフが毎日、1階〜7階の重量（kg）を実測してシートへ手書きで記録する
3. 月が終わるとExcelへ転記する
4. 毎年4月に、前年度（4月〜当年3月）の年度集計を役所へ報告する

これをシステム化し、①手書きシートのスキャン読み取り→②内容の確認・訂正→③月別・年度別集計の自動化、
を実現する。

### 今回の範囲に含むもの

- 手書きシート（1ヶ月分・1階〜7階×日次）のスキャン画像からの実測値読み取り（Claude Vision）
- 読み取り結果の確認・訂正画面（異常値のハイライト付き）
- 手入力での記録・訂正（スキャンを使わない運用も可能にする）
- 月別集計・年度集計（4月始まり3月締め）
- 役所提出用の集計表出力（様式は要確認。13章参照）

### 今回の範囲に**含めない**もの（要確認）

- 一般廃棄物以外の区分（産業廃棄物等）
- BKBビル以外の建物
- 過去分（紙で記録済みの既存データ）のスキャン移行（依頼があれば別途）

---

## 2. 現行の紙シートの分析（添付PDFより）

- タイトル: `■ BKBビル　廃棄物実測集計表　{年}年　{月}月　Kg`
- 列: 日・曜日・一般廃棄物（1階〜7階の7列）・合計
- 行: その月の日数分（28〜31行）＋末尾に「合計」行
- 日曜日の行は赤字（祝日の色分けは無い＝土曜の色分けも無い。既存の他機能の赤=日祝・青=土という配色とは別物）
- 単位は kg。小数点の有無は現物のシートで要確認（実測なので端数が出る可能性あり）

---

## 3. 全体方針

### 3-1. スキャンは「1ヶ月分のシート1枚」を丸ごと読み取る

現行運用が「1ヶ月1枚のシート」である以上、システム側もこれに合わせ、記入済みシートを
1枚の写真（またはスキャンPDF）としてアップロードし、Claude Vision で日×階のマス目を
まとめて読み取る。違反車両のナンバープレート読み取り（`recognizeVehicle`。手動トリガー・
「読み取れないものは推測せず null」という考え方）と同じ設計を踏襲する。

### 3-2. 読み取り結果は「未確認」のまま保存し、必ず人が確認してから確定する

役所への報告に使う数値のため、OCR結果を無条件に確定させない。読み取り直後は
`is_confirmed = false` の下書き状態にし、確認・訂正画面で人が内容を見てから確定する
（確定後も後から編集は可能）。

### 3-3. 異常値の判定基準

依頼元の実務ルール（「通常は15kg未満、20kgを超えるとかなり多め、50kg以上は書き間違い」）を
そのまま実装する。**自動修正はしない**（本当にその日だけ多かった可能性もあるため）。
確認画面で色分けして人の目に留まるようにするだけで、保存自体は妨げない。

| 範囲 | 表示 |
|---|---|
| 0kg 以上 20kg 以下 | 通常表示 |
| 20kg 超 50kg 未満 | 注意表示（オレンジ。「多め」） |
| 50kg 以上 | 警告表示（赤。「書き間違いの疑い」） |

### 3-4. 年度は4月始まり3月締め

日報・備品等の既存機能は暦年（1月始まり）だが、廃棄物は役所報告の単位に合わせて
**年度（4月〜翌3月）** を基準に集計・表示する（既存機能に無い新しい概念のため、
コード上は「年度」専用のユーティリティを新設する）。

---

## 4. データモデル（Supabase。案）

```sql
-- 廃棄物の実測値（1日1階＝1行。既存の残留塩素・ビルメン予定と同じ「正規化した行＋
-- 一意制約」の考え方）
create table waste_records (
  id            uuid primary key default gen_random_uuid(),
  record_date   date not null,
  floor         text not null,           -- '1'〜'7'（EQUIPMENT_FLOOR_OPTIONS のうち B を除いた範囲）
  weight_kg     numeric(6,2) not null,
  source        text not null default 'manual' check (source in ('manual', 'ocr')),
  is_confirmed  boolean not null default true,  -- OCR取込直後だけ false。手入力は常に true
  scan_id       uuid references waste_scans(id) on delete set null, -- どの取込由来か（任意）
  note          text,
  created_at    timestamptz not null default now(),
  updated_at    timestamptz not null default now(),
  unique (record_date, floor)
);

-- スキャン取込のバッチ（1回のアップロード＝1ヶ月分）
create table waste_scans (
  id           uuid primary key default gen_random_uuid(),
  target_month text not null,             -- 'YYYY-MM'（読み取り対象の月）
  storage_key  text not null,             -- アップロードした画像
  status       text not null default 'pending' check (status in ('pending', 'reviewed')),
  raw_result   jsonb,                     -- Claude の生の読み取り結果（監査用に保持）
  reviewed_by  text,
  reviewed_at  timestamptz,
  created_at   timestamptz not null default now()
);
```

`api_usage` に `waste_calls`（違反車両の `parking_calls` と同じ考え方の内訳列）を追加する。

---

## 5. 画面設計（案）

### 5-1. 一覧 `/waste`

既存の残留塩素等検査（`Chlorine.jsx`）と同じ「年月グループ＋トグル開閉」の一覧にする。
各月グループの中は、添付PDFと同じ形（日×1〜7階の表＋日合計列＋月合計行）。

- ヘッダーに「Excelアップロード」ボタン（対象月を選んでアップロード。**2026-09-09にスキャン取込
  〈Claude Vision〉から変更。10-5章参照**）
- 一覧上でその場編集も可能（残留塩素と同じ、既存データの微修正用）
- 異常値（20kg超・50kg以上）は一覧表示でも色分けする

### 5-2. Excel取込モーダル（2026-09-09改訂。旧: スキャン取込モーダル）

> 当初案（Claude Visionによる手書きシートの写真読み取り）は10-5章のとおり実際の筆跡で
> 読み取り失敗が続いたため、依頼元がExcel化した実測値をそのまま取り込む方式に変更した。
> 以下は変更後の設計。

1. 対象月を選び、2章の様式（日・曜日・1〜7階・合計の列を持つ）のExcelファイル（.xlsx）を選択
2. 「取り込む」を押すと、ブラウザ内で直接Excelを読み取り（`src/lib/wasteExcelImport.js`。
   サーバーへは元ファイルを送らず、パース済みの日×階の値だけを送る）、
   `waste_records` へ `is_confirmed=false` の下書きとして保存する
3. 結果は一覧（月別表示）にそのまま反映される。この画面自体には読み取り結果のプレビューは
   出さない（月別一覧の編集可能グリッドが確認・訂正の場を兼ねる。10章の設計をそのまま踏襲）
4. 異常値は一覧側で色分けして表示。人が確認し、必要な行は手で訂正
5. 一覧の「この月を確認済みにする」ボタンで、直していない残りをまとめて `is_confirmed=true` にする

### 5-3. 年度集計 `/waste`（年度切替）または別画面

年度（4月〜翌3月）を選ぶと、月別×階別の合計表と年度合計を表示する。
**役所提出書類そのものへの出力は行わない**（9章の回答のとおり、提出様式は他の入力項目が
大半を占める中の一箇所に過ぎないため、依頼元が年度合計値を見て手入力する運用。
画面はその値を迷わず拾える表示になっていればよい）。

---

## 6. API設計（Worker。既存パターンに合わせる）

- `GET/POST/PATCH/DELETE /api/waste/records`: 実測値のCRUD（平たいパス＋idはクエリ/ボディ）
- `GET /api/waste/records?fiscal_year=2026`: 年度集計用の全件取得
- `POST /api/waste/records/import`（2026-09-09〜。10-5章）: ブラウザ側で読み取り済みの
  Excel由来の行データ（`{ rows: [{ record_date, floor, weight_kg }] }`）をまとめて
  `is_confirmed=false` の下書きとして upsert する。旧`POST /api/waste/scans`・
  `POST /api/waste/scans/recognize`（画像アップロード・Claude Vision読み取り）は廃止した

---

## 7. 権限

既存機能と同じ（owner は閲覧のみ、社員は読み書き）を踏襲する想定。異論があれば教えてほしい。

---

## 8. 開発フェーズ（案）

| Phase | 内容 |
|---|---|
| 1 | データモデル・一覧・手入力・その場編集・月次集計 |
| 2 | スキャン取込（Claude Vision）・確認/訂正画面・異常値ハイライト |
| 3 | 年度集計画面（月別×階別＋年度合計の表示のみ。役所提出書類への出力機能は作らない） |

---

## 9. 確認事項（2026-09-03 回答済み）

1. **スキャン取込の単位**: 「1ヶ月分のシート1枚」を丸ごと読み取る方式で確定
2. **異常値の扱い**: 色分け表示のみで保存自体は妨げない、で確定
3. **役所提出様式**: 依頼元は様式を保有しているが、**年度合計値は様式内の多数の入力項目の
   うちの1箇所に過ぎないため、依頼元が手入力する運用でよい**とのこと。
   → 本システムでは役所提出書類そのものへの出力機能は作らず、年度集計画面で
   月別×階別の合計と年度合計を分かりやすく表示するところまでを範囲とする
4. **対象範囲**: BKBビル・一般廃棄物のみで確定

---

## 10. 実装内容（2026-09-03。Phase 1〜3を一括実装）

計画の3フェーズを1回のセッションでまとめて実装した。

| 追加・変更したもの | 内容 |
|---|---|
| Supabase（本番へ直接適用） | `waste_records`（実測値）／`waste_scans`（取込バッチ）を追加。`api_usage` に `waste_calls` 列、`add_api_usage` に `p_waste_calls` 引数を追加 |
| `worker/lib/anthropic.js` | `recognizeWasteSheet(imageBase64, mediaType, { month, floors })`。`recognizeVehicle` と同じ「判読できないマスは推測せず null」方式。「合計」列・行は読み取らせず、日次実測値だけを `{ days: { "1": { "1": 12.5, ... } } }` 形式で返させる |
| `worker/lib/waste.js` | 実測値のCRUD・スキャンのアップロード／読み取りハンドラ。権限は残留塩素・自主検査と同じ `requireAuth(req, { write: true })`（owner・備品出庫限定ロールは閲覧のみ） |
| `worker/index.js` | `/api/waste/records`・`/api/waste/records/confirm-month`・`/api/waste/scans`・`/api/waste/scans/recognize` をルーティング |
| `src/lib/waste.js` | API呼び出し、`classifyWasteWeight`（異常値判定）、年度（4月始まり3月締め）のユーティリティ |
| `src/lib/imageResize.js` | `waste` プリセットを追加（`maxEdge: 2000`）。日×7階の細かい手書き数字が密集するため、他の用途より高い解像度で送る |
| `src/components/Icons.jsx` | `IconScale`（天秤）を追加。既存の `IconTrash`（削除ボタン専用）を「廃棄物」の意味で流用すると紛らわしいため別アイコンにした |
| `src/pages/Waste.jsx`・`Waste.css` | 一覧兼確認・訂正画面（`/waste`）。月別表示は添付PDFと同じ日×1〜7階の表をそのまま**編集可能なグリッド**として再現し、これ1つでスキャン結果の確認・訂正と手入力の両方を受け止める（別画面の「確認・訂正画面」は作らず統合）。年度集計はビューを切り替えて表示（月別×階別＋年度合計。出力機能なし） |
| `src/components/WasteScanModal.jsx` | スキャン取込モーダル（対象月選択→撮影/ライブラリ選択→アップロード→読み取り実行） |
| `src/App.jsx`／`AppHeader.jsx`／`Portal.jsx` | `/waste` ルート追加。ハンバーガーの共通メニュー（第3領域）に「廃棄物」を追加（owner も閲覧できるため他の共通メニュー項目と違い隠さない）。ダッシュボードに廃棄物カード（当月合計・未確認件数）を追加 |

**計画から変えた点**:

- 5-2で想定していた「読み取り結果をプレビューしてから確定」という**専用の確認画面は作らなかった**。
  代わりに月別表示の編集可能グリッドをそのまま確認・訂正の場として使う（スキャン直後のマスは
  点線枠で「未確認」を示す。値を直接編集すれば自動的に確認済み扱いになり、直す必要が無かった
  残りは「この月を確認済みにする」ボタンでまとめて確認済みにできる）。画面を1つに絞ることで
  実装・運用の両方をシンプルにした
- `waste_scans` に計画になかった `mime` 列を追加した（読み取り時にStorageの応答ヘッダーへ頼らず、
  `report_photos.mime` と同じ確実な方式にするため）
- 画像の保存先は新規バケットを作らず、既存の `report-photos` バケットを流用した
  （`waste-scans/` プレフィックスで区別。カテゴリごとにバケットを分けている雛形ファイルとは
  異なり、写真と同じ性質のデータのため）

**実機確認は未実施**（このAI環境にはSupabase/Cloudflareの資格情報はあるがGmail同様ブラウザ操作は
できないため、lint・buildの確認までしか行えていない。特に次の点を実機で確認してほしい）:

- 手書きシートの実際の写真でのスキャン読み取り精度（マスの判読・「合計」列を誤読しないか）
- iPhone幅での編集グリッドの操作感（列数が多いため横スクロールが必要になる想定）
- 異常値の色分け（20kg超・50kg以上）が実データで意図通りに出るか

### 10-1. 記入用シートの印刷機能を追加（2026-09-03。実機確認前の追加依頼）

「スキャンは入力済みシートを入手次第テスト」という回答と同時に、**添付PDFと同じフォーマットの
記入用シートを当システムから印刷できるようにしてほしい**という依頼を受けた。あらかじめ印刷して
配布し、それに手書きしたものを後日スキャンで取り込む運用のため、値は一切入れない空欄のシートを
生成する（既存データは無関係）。

| 追加したもの | 内容 |
|---|---|
| `src/components/WasteSheet.jsx`・`.css` | 記入用の空欄シート。添付PDFと同じ「日・曜日・1〜7階・合計」の表を再現し、日曜・祝日の行だけ赤系で塗る（元の紙シートに合わせ、土曜の色分けは付けない）。`BilmenScheduleSheet.jsx` と同じ画面外描画＋html2canvas方式 |
| `src/hooks/useWasteSheetPdfExport.jsx` | PDF出力フック。月と祝日だけを渡す単純な1ページ出力（値を扱わないため他の帳票フックより単純） |
| `worker/lib/reports.js` | `VALID_PDF_KINDS` に `waste-sheet` を追加（プレビュー保存の許可リスト） |
| `src/pages/Waste.jsx` | ヘッダーに「入力シートを印刷」ボタンを追加。**readOnly（owner・備品出庫限定ロール）でも使える**（値を含まない出力のため、スキャン取込・確認済みにするボタンとは異なり制限しない） |

### 10-2. 月別一覧表：日・曜日列を横スクロールでも固定（2026-09-04）

「モバイル画面で見やすいように」との依頼で、月別一覧（`MonthTable`）の日・曜日の2列を
横スクロールしても常に見える frozen column にした。床数(7)+合計+日+曜日で列数が多く、
iPhone幅では横スクロールが前提になるため、日付を見失わないようにする。

| 変更したもの | 内容 |
|---|---|
| `src/pages/Waste.jsx` | `MonthTable` の `<table>` に `waste-month-table` クラスを追加（年間集計表と区別するため。年間集計表は列数が少なく対象外） |
| `src/pages/Waste.css` | `.waste-month-table` の日・曜日列（1・2列目）に `position: sticky` を適用（`Equipment.css` の入出庫明細表の frozen column と同じ考え方）。固定列は下の行がスクロールで透けないよう、縞模様・合計行それぞれの背景色を個別に持たせた。合計行の先頭セルは `colSpan={2}`（「合計」ラベル）のため、2列目の固定はそれ以外の行に限定（さもないと実際は1階列である2番目の`td`がずれて固定されてしまう） |

実機確認は未実施（このAI環境はブラウザ操作ができずlint・buildの確認までのため、iPhone幅での
表示・スクロール時の見た目を実機で確認してほしい）。

### 10-3. 年度集計の簡素化とツールバーのキャプション変更（2026-09-04）

「OKです」の回答とあわせ2件の修正依頼を受けた。

| 依頼 | 対応 |
|---|---|
| 年度集計表示：月毎の合計のみを表示（1〜7階の小計は不要）。年度合計は表の上に独立して太字で表示 | `YearTable` を月別合計だけの2列（月・合計）に簡素化し、階別の内訳列と表内の合計行を廃止。年度合計は表の上に `.waste-year-total` カード（ポータルの統計値と同じ `--fs-4xl` で大きく表示）として独立表示するようにした |
| iPhoneでの月別表示：ボタンのキャプションに「入力用フォーム」「スキャン」をつけてアイコンは省略 | 「入力シートを印刷」「スキャン取込」の2ボタンからアイコン（`IconDocument`）を外し、キャプションを短縮した「入力用フォーム」「スキャン」に変更。共通ルール（`.btn-plain-label` を持つボタンは480px以下でアイコンのみへ縮む。`ui.css` 2章）の対象から外れるよう、ラベルを `.btn-plain-label` ではなくボタンの直接のテキストにした（このクラスを使うとiPhone幅で共通ルールによりラベルごと非表示になり、アイコンも無いため空のボタンになってしまうため） |

実機確認は未実施（このAI環境はブラウザ操作ができずlint・buildの確認までしか行えていない）。

### 10-4. 従量課金事項の集計に廃棄物スキャンが含まれていなかった不具合を修正（2026-09-04）

依頼元から「APIクレジット不足の警告が出たが、従量課金事項の推定コストと差が大きい」との
問い合わせを受けて調査した過程で発見した不具合。10章でスキャン取込機能を追加した際、
`api_usage`テーブルに`waste_calls`列とその加算ロジック（`add_api_usage`関数）は追加したが、
`worker/index.js`の`handleUsage`（`/api/usage`）が返す列に`waste_calls`を含め忘れていた。
その結果、`src/components/UsagePanel.jsx`の内訳計算（`総件数 - FAX件数 - 車両画像件数`で
「メール」件数を算出）が廃棄物スキャンの件数を差し引けず、**スキャン取込の件数が「メール」の
件数として誤表示されていた**（入力・出力トークンの合計、および推定コストの金額自体は
`input_tokens`/`output_tokens`という共通の集計列を使っているため、この不具合の影響を受けず
正しかった）。`handleUsage`の取得列に`waste_calls`を追加し、`UsagePanel.jsx`に「廃棄物」列を
新設して正しく分離した。

### 10-5. スキャン取込（Claude Vision）をExcelアップロードに置き換え（2026-09-09）

依頼元が実際に記入済みの手書きシートでスキャン取込を試したところ「読み取り失敗」となった。
別途AIチャット等で手書き内容をExcel化したところ正しく変換できたとのことで、**そのExcel
ファイルをそのまま読み取る方式に変更してほしい**との依頼を受けた（実際のExcelファイルを
添付いただき、これを新方式の参照フォーマットとした）。

| 変更したもの | 内容 |
|---|---|
| `src/lib/wasteExcelImport.js`（新規） | Excel（.xlsx）ファイルをブラウザ内で直接読み取るパーサー。xlsxはOOXML＝ZIP形式のため、本来はライブラリ（SheetJSの`xlsx`等）を使うのが簡単だが、npm公開版の`xlsx`パッケージは未修正の高リスク脆弱性（プロトタイプ汚染・ReDoS）を抱えており、修正版はSheetJS自社CDN配布のみでこの開発環境からは取得できなかった（`exceljs`は依存過多で見送り）。読み取る値が「日×1〜7階の実測値」だけで形式も固定のため、依存追加を避け、ZIP展開（中央ディレクトリ・ローカルヘッダの手動パース＋`DecompressionStream('deflate-raw')`での展開）とシートXMLの数値セル読み取りだけを自前実装した。実際に依頼元から添付されたExcelファイルで読み取り値が原本の列合計と完全一致することを確認済み |
| `src/components/WasteExcelImportModal.jsx`（新規。`WasteScanModal.jsx`を置き換え） | 対象月選択＋.xlsxファイル選択の2ステップのみのシンプルなモーダル。選んだファイルはブラウザ内でパースし、日×階の値だけをサーバーへ送る（元ファイル自体はアップロードしない） |
| `worker/lib/waste.js` | `handleWasteScanUpload`・`handleWasteScanRecognize`（画像アップロード・Claude Vision読み取り）を削除し、`handleWasteRecordImport`（`POST /api/waste/records/import`）を新設。パース済みの行を検証し`is_confirmed=false`の下書きとしてupsertするだけの単純な処理になった（Storageへの画像保存・AI呼び出し・利用量記録が丸ごと不要になった） |
| `worker/lib/ai/`（`index.js`・`anthropic.js`・`prompts.js`） | `recognizeWasteSheet`・`buildWasteSystemPrompt`を削除（廃棄物のAI読み取り自体が無くなったため） |
| Supabase（本番へ直接適用） | `waste_records.source`のCHECK制約を`('manual','ocr')`から`('manual','excel')`に変更（`ocr`のレコードは0件だったため置き換え。`waste_scans`テーブル自体は履歴として残し削除していない） |
| `src/lib/imageResize.js` | `waste`プリセット（`WasteScanModal.jsx`専用だった高解像度設定）を削除 |
| `src/pages/Waste.jsx` | ヘッダーのボタンを「スキャン」→「Excelアップロード」に変更。月別一覧の編集可能グリッド・確認済みにするボタン等、既存のUI・確認フローはそのまま流用（取込元だけが変わった） |

**サーキットブレーカー（3-3・10-4章）の対象からも外れる**: 廃棄物はAIを一切呼ばなくなったため、
`docs/task-management-spec-cloudflare.md`の日次AI利用上限の説明から廃棄物を除外した。

実機確認はまだ未実施（読み取りロジック自体は依頼元から提供された実際のExcelファイルで
数値が完全一致することを確認済みだが、モーダルの操作感・実際のアップロード〜一覧反映までの
一連の流れは依頼元に確認してほしい）。

# Anthropic API の従量課金に関する検討（2026-08-06 / 2026-09-04 / 2026-09-05 追記）

> **【2026-09-04 追記】この文書の1章以降は、平常運転時（1日あたり$0.06〜0.08）の実績に基づく
> 検討である。その後、不具合により1日で約20倍を消費してクレジットを枯渇させる事故が起き、
> 原因の修正と再発時の歯止めを実装した。まず下記「9章 2026-09-04の課金事故と再発防止」を
> 読むこと。**
>
> **【2026-09-05 追記】** 社長が従量課金の発生自体に消極的なため、**Gemini（無料枠）への
> 乗り換えを再検討**した（5章の結論の見直し）。結果は「**10章 Gemini（無料枠）への乗り換え
> 再検討**」にまとめてある。5章より新しいので、Geminiの検討は10章を読むこと。
> 実際に切り替える場合の計画は「**11章 Gemini 有料枠へ切り替える場合の実装計画**」にある。

社長への説明で「従量課金であること自体」がネックになっている件について、実測値の整理と
代替手段の比較を行った。**結論は「他社サービスへの乗り換え」ではなく「支払い方式を前払い・
上限付きに変えること」を推奨する**。理由は本文のとおり。

---

## 1. 現状の実測値（推測ではなく `api_usage` テーブルの実データ）

| 月 | 分類件数 | 内訳 | 入力トークン | 出力トークン | 実費 |
|---|---:|---|---:|---:|---:|
| 2026-07（丸1ヶ月） | 187件 | メール163 / FAX24 / 車両0 | 592,740 | 46,588 | **$0.83（約¥128）** |
| 2026-08（6日時点） | 18件 | メール17 / FAX1 / 車両0 | 75,430 | 5,453 | $0.10（約¥16） |
| 累計 | 205件 | — | 668,170 | 52,041 | $0.92（約¥143） |

単価は Claude Haiku 4.5 の $1.00/100万入力トークン・$5.00/100万出力トークン（2026-08時点の
公式価格。`src/lib/pricing.js` の設定値と一致していることを確認済み）。

**ここから導かれる数字:**

| 指標 | 値 |
|---|---|
| 1件あたりの単価 | **約0.7円** |
| 月額（実績ベース） | **約130円** |
| 年額（実績ベース） | **約1,500円** |
| 3年分をまとめて前払いする場合 | **約5,000円（$30）** |

社長への説明で使うなら、「従量課金」という言葉より **「1件0.7円」「年間1,500円」** という
実数のほうが伝わる可能性が高い。参考までに、同じ業務を人手でやると1件あたり数十秒〜数分の
判断作業になる。

---

## 2. 論点の整理 — 問題は金額ではなく「青天井に見えること」

年間1,500円という金額自体が問題になっているとは考えにくい。従量課金が敬遠される本質的な
理由は **「使った分だけ請求される ＝ 上限が読めない ＝ 予算化できない」** という点にあるはず。

だとすれば、解くべき課題は「Anthropicをやめること」ではなく **「上限が保証された、
予算化できる支出に変えること」** になる。そして後述のとおり、これはコードを1行も変えずに
実現できる。

---

## 3. 案1: 前払い・上限付きに変更する【推奨・コード変更なし】

### 実は既に「前払い」である

**Anthropic API は元々クレジット前払い方式**で、月末に請求書が届く後払いの従量課金ではない。
先にクレジットを購入し、使うと残高が減っていく仕組み（プリペイドカードと同じ）。現在も
$5を購入した残高で運用している。

さらに、コンソール側で以下の2つを設定すると **構造的に「上限を超えた請求」が発生しなくなる**:

| 設定 | 場所 | 効果 |
|---|---|---|
| **オートリロードをOFF** | Anthropic Console → Billing | 残高が尽きたらそこで停止。カードへの追加請求が一切発生しない |
| **月間上限（spending limit）を設定** | Anthropic Console → Limits | 上限に達するとオートリロードが発火しない（二重の歯止め） |

オートリロードをOFFにすれば、**購入した金額が文字通りの上限**になる。「¥5,000を1回だけ
購入し、それ以上は何があっても課金されない」という状態が作れる。これは会計上、従量課金
ではなく **消耗品の前払い購入** と同じ扱いにできる。

### 残高が尽きた時の挙動は実装済み

本システムは既にクレジット不足を検知して安全に停止する実装を持っている（コード変更不要）:

- `worker/lib/anthropic.js` の `isBillingError` 判定（402、および400の "credit balance is too low"）
- `worker/lib/pipeline.js` がクレジット不足を検知すると `settings.api_credit_alert` を立てる
- メイン画面に「⚠️ APIクレジットが不足し、メールの自動分類が停止しています」の警告バナーを表示
- 残高が復活すれば自動でアラート解除

つまり **「残高が尽きる ＝ 静かに壊れる」ではなく「残高が尽きる ＝ 画面に警告が出て、
チャージするまで自動分類だけが止まる」** という設計になっている。タスク管理・日報などの
他機能は影響を受けずに使い続けられる。

### 社長への説明案

> AIの利用料は「使った分だけ後から請求される」形式ではなく、**プリペイド式**です。
> 先に¥5,000分を購入すると、それが上限になります。オートチャージは切ってあるので、
> 万が一使いすぎても追加請求は発生しません。実績ベースでは¥5,000で約3年分に相当します。
> 残高が尽きた場合は画面に警告が出て、AI自動分類だけが停止します（他の機能は動きます）。

**この案のメリット:** コード変更ゼロ、品質劣化ゼロ、移行リスクゼロ、作業は
コンソールの設定変更のみ（5分程度）。

---

## 4. 案2: Cloudflare Workers AI に切り替える【部分的には可能。品質リスクあり】

本システムは既に Cloudflare Workers 上で動いているため、Workers AI は技術的な相性が最も良い。

| 項目 | 内容 |
|---|---|
| 無料枠 | **10,000 Neurons/日**（クレジットカード登録不要・恒久的な無料枠） |
| 当社の想定使用量 | 1日あたり約6件 → 無料枠の数%程度。**課金は発生しない見込み** |
| 利用可能モデル | Llama 3.3、Llama 3.2 Vision（11B）、Qwen系、Moondream 3（OCR対応の軽量VLM）など |
| 課金形態 | 無料枠内なら**完全無料**（＝従量課金の問題が消える） |

### ただし、品質面に無視できないリスクがある

現在Claudeにやらせている仕事は、単なる翻訳や要約ではなく以下の3つ:

1. **日本語ビジネスメールの業務判定と担当者振り分け** — `org_context`（社内の業務背景・
   振り分けルール）を読んで判断する必要がある。オープンモデルでも一定はできるが、
   ルールの解釈精度は落ちる可能性が高い。
2. **FAXのスキャン画像の読み取り** — ここが最大の懸念。複合機でスキャンした日本語FAXは
   かすれ・低解像度・手書きが混在する。Claudeでも読み取りに苦労するため、
   ハルシネーション対策（判読できなければnullを返す方針）をわざわざ実装した経緯がある
   （設計書4-3章、HANDOFF「経緯の要約」34番）。**7B〜11B級のオープンモデルの日本語OCR精度は
   Claudeより明確に劣る**というのが一般的な評価で、劣化幅は実際に試さないと分からない。
3. **違反車両のナンバープレート読み取り** — 同上。

### 現実的な使い方（ハイブリッド案）

もしWorkers AIを試すなら、いきなり全部を置き換えるのではなく **難易度の低いところから
段階的に** が安全:

| 処理 | 難易度 | Workers AIへの移行可否 |
|---|---|---|
| 通常メールの業務判定・振り分け（テキストのみ） | 中 | 試す価値あり。精度比較しやすい |
| FAX画像の読み取り | 高 | **非推奨**。誤読が業務に直結する |
| 違反車両のナンバー読み取り | 高 | **非推奨**。同上 |

ただし、この案には根本的な弱点がある。**FAX・車両画像でClaudeを残す限り、Anthropicの
課金アカウント自体は残る** ため、「従量課金をなくす」という当初の目的は達成できない。
月額は下がるが（メール分類が入力トークンの大半を占めるため、おそらく半額以下）、
それは元々130円の話なので、社長への説明という観点ではほとんど意味がない。

**評価: 目的（従量課金の解消）に対して、労力と品質リスクが見合わない。**

---

## 5. 案3: Google Gemini の無料枠【機密性の観点から非推奨】

| 項目 | 内容 |
|---|---|
| 無料枠 | Gemini 2.5 Flash で 10 RPM / 250リクエスト/日 |
| 当社の想定使用量 | 1日6件程度 → 余裕で無料枠内 |
| 日本語・画像認識の品質 | Claudeに近い水準が期待できる（オープンモデルより明確に上） |
| 課金形態 | 完全無料 |

品質と無料枠の広さだけを見れば最も魅力的に見えるが、**採用すべきでない決定的な理由がある。**

> **Gemini API の無料枠は、送信したプロンプトとレスポンスがGoogleの製品改善（モデルの学習）に
> 利用される可能性がある。** 有料枠（Tier 1以上）に上げるとこの制約は外れるが、有料枠は
> 従量課金なので当初の目的に戻ってしまう。

本システムがAIに送っているのは以下の内容:

- 顧客からの問い合わせメール全文（会社名・担当者名・メールアドレス・電話番号）
- 注文書・見積書・請求書のPDF/FAX画像（**金額・数量・納期・品番**）
- 違反車両の写真（ナンバープレート）

これらは顧客の営業情報そのものであり、外部の学習データに流れる可能性を許容するのは、
2026-07-15のセキュリティ審査で「anon/authenticatedからは一切アクセス不可」まで
厳格化した現在の運用方針（設計書8章）と明確に矛盾する。

**評価: 品質・費用は魅力的だが、機密保持の観点で採用不可。**

---

## 6. 案4: 自前ホスティング / 国産AI-OCRの定額プラン【費用が跳ね上がる】

「完全な定額にする」という観点では以下も選択肢になるが、いずれも費用が大幅に増える。

| 手段 | 月額の目安 | 現状比 | 評価 |
|---|---:|---:|---|
| GPU付きVPSでオープンモデルを自前運用 | ¥10,000〜30,000 | 約80〜230倍 | 保守の手間も発生。非現実的 |
| 国産AI-OCRサービスの定額プラン | ¥30,000前後が相場 | 約230倍 | OCRしかできず、業務判定・振り分けは別途必要 |
| Claude の Batch API（50%割引） | 約¥65 | 半額 | 最大1時間の遅延を許容する必要あり。**年間約770円の節約のために非同期処理を実装するのは割に合わない** |

補足として **プロンプトキャッシュ**（同じシステムプロンプトの再利用で入力料金を約1/10にする機能）も
検討したが、**Haiku 4.5 のキャッシュ最小単位は4,096トークン**で、当システムの1リクエストあたりの
平均入力は約3,170トークン（システムプロンプト部分はそれ以下）。**最小単位に届かないため
キャッシュが作動せず、効果がない。**

**評価: いずれも費用対効果が成立しない。**

---

## 7. 推奨と次のアクション

### 推奨: 案1（前払い・上限付きへの変更）

理由:

1. **コード変更が不要** — 移行リスク・品質劣化リスクがゼロ
2. **社長の懸念（青天井であること）を構造的に解消できる** — オートリロードOFFにより、
   購入額を超える請求が物理的に発生しない
3. 他案はいずれも、品質劣化（案2）・機密性リスク（案3）・大幅な費用増（案4）のいずれかを伴う
4. 現在の実費が年間1,500円であり、**乗り換えによって節約できる最大額も年間1,500円**。
   どれだけ努力しても、得られるものが小さい

### 具体的な作業手順（担当: 管理者アカウント保有者）

1. Anthropic Console → **Billing** → オートリロードが有効になっていれば **無効化**
2. Anthropic Console → **Limits** → 月間の上限額を設定（例: $5 = 約¥775）
3. 必要に応じてクレジットを追加購入（¥5,000 ≒ $30 ≒ 実績ベースで約3年分）
4. 上記を社長に「プリペイド式・上限¥○○」として説明

### 併せて検討してもよいこと

- **違反車両のAI読み取りは手動トリガー式**（写真ごとにボタンを押したときだけ実行）なので、
  使用量が意図せず増える構造にはなっていない。この点も説明材料になる
- 従量課金事項の画面で**月別の実績と円換算額が常時確認できる**ようになっている（2026-08-05実装）。
  「毎月いくら使ったかが画面で見える」ことも安心材料として提示できる

### 案2を試すとしたら（優先度: 低）

もし将来的に「Anthropicへの依存自体を減らしたい」という別の動機が出てきた場合は、
Cloudflare Workers AI で **通常メールのテキスト分類のみ** を試験的に並走させ、
Claudeの判定結果と突き合わせて精度を比較する、というのが最も安全な進め方になる。
その場合もFAX・車両画像の読み取りはClaudeに残すことを強く推奨する。

---

## 8. この検討の前提と限界

- 実費の算出は当システムが計測したトークン数に基づく試算であり、Anthropicの確定請求額とは
  若干ずれる可能性がある（ただし当APIキーは本システム専用のため、ほぼ一致するはず）
- 案2・案3の品質評価は公開情報とモデルの一般的な特性に基づく推定であり、**当社の実際のFAX画像で
  比較検証したものではない**。採用を検討する場合は、実際の帳票数十件で読み取り精度を
  比較する検証が別途必要
- 為替は1ドル155円で換算。円安が進めば円換算額は上振れする（ただし元の金額が小さいため
  影響は限定的）

---

## 9. 2026-09-04の課金事故と再発防止（HANDOFF 219〜225番）

### 何が起きたか

$5のクレジットが2日で枯渇した。日別の実績は次のとおり（Anthropicコンソールの実測値）。

| 日（UTC） | コスト | 備考 |
|---|---:|---|
| 8/31 | $0.06 | 平常 |
| 9/1 | $0.07 | 平常 |
| 9/2 | $0.08 | 平常 |
| **9/3** | **$1.27** | 約16〜20倍 |
| **9/4** | **$1.37** | 同上。この日にクレジットが枯渇 |

### 原因（2つの不具合の重なり）

> **【2026-09-04 訂正】** 当初この節は「9/3・9/4に添付付きの営業メールが集中して顕在化した」
> と記述していたが、実データの精査により**誤りと判明した**ため全面的に書き直した。
> 増えたのはメール件数ではなく**実行回数**である。詳細は下記「原因B」。

事故は**単独のバグではなく、2つの不具合が重なって初めて成立した**。片方だけなら
平常運転の範囲に収まる。ここを取り違えると再発防止の的を外すため、順に説明する。

#### 原因A: 業務外メールが「判定済み」として記録されなかった

`worker/lib/pipeline.js` の業務外判定が、**タスクを作らずに `continue` で打ち切っていた**。
重複判定は `tasks.gmail_message_id` を見ているため、タスクにならない業務外メールは
「判定済み」の記録がどこにも残らず、**取得クエリに入り続ける限り何度でも再分類される**
状態だった。2026-08-24に取得クエリへ `in:spam` を追加して以降、スパムは必ず業務外＝
この対象になっていた。

ただし**原因Aだけでは事故にならない**。取得クエリは `after:<last_fetch_at>` で窓を切って
おり、`last_fetch_at` が毎回前進していれば、同じメールが窓に入るのは1回きりだからである。
事実、8/24〜9/2の9日間はこの状態のまま日次コスト$0.06〜0.08で推移している。

#### 原因B: `last_fetch_at` が前進しなくなった（これが引き金）

9/3、`settings.last_fetch_at` が **`2026-09-02T23:35:46Z` のまま丸一日更新されなかった**。
日次バックアップの `settings` スナップショットで確認できる。

| バックアップ | `last_fetch_at` |
|---|---|
| 9/2分 | `2026-09-02T08:50:39Z`（正常に前進） |
| **9/3分**（9/3 20:53 UTC取得） | **`2026-09-02T23:35:46Z`（丸一日更新なし）** |
| 9/4分 | `2026-09-04T08:40:50Z`（復旧） |

`last_fetch_at` の更新は `runPipeline` の最終手順（手順7）にあるため、実行が途中で
終わると前進しない。前進しなくなると、**独立した2つの経路**で費用が跳ね上がる。

| # | 箇所 | 挙動 |
|---|---|---|
| B-1 | 更新間隔ゲート `if (!force && lastFetchAt)` | 経過時間が常に間隔を超えるため**ゲートが素通り**する。cronは`*/5`固定なので、実行が**30分に1回 → 5分に1回**（1日19回 → 約120回）になる |
| B-2 | 取得クエリ `after:${lastFetchAt}` | 窓が固定され**毎回まったく同じメールが返る**。原因Aと合わさり、同じ業務外メールが5分ごとに永久に再分類される |

処理ログにそのまま残っている（9/3 08:01〜08:15 UTC。取得件数も入力トークン数も完全に同一）:

| 時刻(UTC) | 取得 | 業務外 | 入力トークン |
|---|---:|---:|---:|
| 08:01:03 | 6件 | 2件 | 9,450 |
| 08:05:54 | 6件 | 2件 | 9,450 |
| 08:10:54 | 6件 | 2件 | 9,450 |
| 08:15:56 | 6件 | 2件 | 9,450 |

**金額もこれで説明がつく。** 1実行あたり9,450入力＋約530出力＝**$0.0121**。
稼働時間8〜18時（JST）＝10時間＝cron 120回。**120 × $0.0121 ≒ $1.45**
（実測$1.27。一部の実行は分類前に終了しているため実測はやや下回る）。

つまり**添付PDFは事故の要件ではなかった**。9/3に再分類され続けていたのは
**添付のない平文メール2通**であり、それを1日中回すだけで$1.27に達する。

#### なぜ手順7に到達しなかったのか（2026-09-05に確定）

> この節は当初「未確定（Cloudflare Workers の実行リソース上限と推定）」としていた。
> 依頼元がCloudflareからダウンロードした観測ログ（9/3 17:03〜23:59 JST・1,000行）で
> **確定した**ため書き直した。

**原因は `Too many subrequests by single Worker invocation`**。Cloudflare Workers には
「1回のWorker呼び出しで出せる外部リクエスト（サブリクエスト）の数」に上限があり、
**無料プランは50回**（Workers Paid は1000回）。稼働時間内の巡回は毎回この上限に達しており、
そこから先の処理がすべて失敗していた。

ログに残っていた失敗の内訳（9/3 17:20〜17:55 の各実行）:

| 失敗した処理 | 業務への影響 |
|---|---|
| `reply-check` | **返信検知が動かない**（顧客からの返信がタスクに反映されない） |
| `calendar` | カレンダー由来のタスクが登録されない |
| `archive` | 完了タスクがアーカイブへ移らない |
| `usage` | **Claude利用量が記録されない** → 従量課金事項の画面が実額より少なく出る |
| 操作ログの書き込み | **異常が画面に一切残らない**（9/3の`fetch`ログが4件しか無かった理由） |
| 手順7（`last_fetch_at` の更新） | **取得窓が凍結する** ＝ 原因B |

つまり因果は最後までつながる:

```
サブリクエスト上限(50)を超過
  → 手順6・7が実行されない
  → last_fetch_at が前進しない（原因B）
  → ①間隔ゲート素通り（30分→5分間隔） ②取得窓が固定され同じメールが返る
  → 原因A（業務外メールが判定済みとして記録されない）と合わさって毎回再分類
  → $1.27/日
```

**なぜ50回も使っていたのか**（9/3時点の1巡回あたりの概算）:

| 処理 | 回数 |
|---|---:|
| 返信検知（進行中タスク30件 × スレッド読み取り1回） | **30** |
| メール本文の取得（取得6〜9件。※処理済みでも本文を取ってから重複判定していた） | 6〜9 |
| Claude分類（業務外2件） | 2 |
| 設定・タスク・利用量などのDB読み書き、アクセストークン、添付、カレンダー | 10前後 |
| **合計** | **50前後** |

決定打は**返信検知**である。進行中（未処理・返信済み）タスク1件につきGmailスレッドを
1回読むため、**タスクが増えるほど上限に近づく**。つまりこの事故は、機能追加とは無関係に
「タスクが順調に溜まってきたこと」で必然的に起きる時限式の不具合だった。

同じログで、実行時間も平常の0.3〜1秒に対し**16〜22秒**に伸びていた。

### 切り分けの経緯（同種の調査をするときの参考）

当初は「本システム以外の利用では」と疑ったが、**Anthropicコンソールのモデル内訳が
`Claude Haiku 4.5` のみ**だったことで本システム由来と確定できた（本システムは Haiku しか
呼ばない。Claude Codeでの開発作業は Opus/Sonnet を使うため別物）。**原因の切り分けでは、
まずモデル内訳と日別コストを見ること。**

**アプリ自身の記録（`api_usage`）を当てにしないこと。** 9月分は322,697入力トークン
（$0.41相当）しか記録しておらず、**実費約$2.6の15%**しか捕捉できていなかった。加算は
手順4にあり、分類の途中で実行が終わると加算前に抜けるため、記録は常に実額以下になる。
逆に `updated_at` が9/3 08:30:57で止まっているのに費用は9/4も発生し続けていた点が、
**「記録を残さずに落ちている実行がある」**と気づく最初の手がかりだった。

**「いつから」を確定させるには日次バックアップの `settings` を見ること。**
`task_management-backups` リポジトリの `data.sql` は日次でコミットされているため、
`last_fetch_at` のような制御用の値を日単位で遡って比較できる。今回の原因Bは
9/2分・9/3分・9/4分の3世代を突き合わせて確定した。

**再分類ループの見分け方**: 処理ログで**取得件数と入力トークン数が複数の実行にわたって
完全に一致**していたら、同じメールを繰り返し分類している。1回あたりの金額 × 稼働時間中の
cron回数が日別コストと合うかどうかで裏を取れる。

### 実装した4段の対策

| 段 | 対応する原因 | 内容 | 実装 |
|---|---|---|---|
| ①原因を塞ぐ | 原因A | 業務外と判定したメールを記録し、二度とClaudeへ送らない | `processed_messages` テーブル。照合は**添付取得・Claude呼び出しより前**に置く |
| ②暴走の回転数を止める | 原因B | 更新間隔ゲートを `last_fetch_at` から切り離し、実行が途中で落ちても次のcron刻みが止まるようにする | `settings.last_run_at` を新設。**Claude呼び出しより前**に記録し、ゲートはこちらで判定する |
| ③上限で止める | 共通 | 1日の推定利用額が上限に達したら、その日のAI処理を自動停止 | `api_usage_daily` ＋ `worker/lib/usageLimit.js`。上限は `settings.daily_api_cost_limit_usd`（既定$0.50・従量課金事項の画面で変更可） |
| ④気づける | 共通 | 停止時にダッシュボードのバナー・**Web Push通知**・処理ログの`error`種別（赤）。加えて**取得窓の凍結**そのものを検知して赤ログに出す | `notifyApiAlert`。凍結検知は `last_run_at` が `last_fetch_at` より間隔の2倍以上先行したら発報（1日1回。`fetch_stall_alert_on`） |
| ⑤上限に当たらないようにする | 手順7に到達しなかった真因 | サブリクエストの使用数を数え、**残りが尽きる前に重い処理を打ち切って次の巡回へ回す**。末尾の記録処理ぶんは常に取り置く | `worker/lib/subrequests.js`（2026-09-05）。上限は `settings.subrequest_limit`（既定50） |

上限の判定を**件数ではなく金額**にしているのは、添付の有無で1件あたりのトークン数が2桁変わり、
件数上限では歯止めにならないため。

⑤の中身（2026-09-05）:

- **処理済み・業務外判定済みのメッセージは、本文を取りに行く前にスキップする**。従来は
  `getMessage` の後に重複判定していたため、取得窓に残っている処理済みメールのぶんだけ
  毎回Gmailを叩いていた（40通なら40回で上限50の大半を消費）。判定材料はメッセージIDだけなので
  取得前に判定できる
- **返信検知は1巡回あたり12件まで**（`REPLY_CHECK_MAX_TASKS`）。「未処理」は対応漏れに直結するので
  毎回全件、「返信済み」は余った枠を `settings.reply_check_cursor` で前回の続きから順に回す
- **予算切れでメールを積み残した回は、`last_fetch_at` を「処理し終えた最後のメールの受信時刻−1秒」
  までしか進めない**。現在時刻まで進めると未処理のメールを永久に取りこぼすため。凍結もしないので
  ④の凍結検知が誤発報しない
- カレンダー登録も予算が無ければ次の巡回へ繰り越す
- `Too many subrequests` を検知したら、処理ログに日本語の説明を添えて赤い `error` で残す。
  繰り越した件数もログ本文に「次回へ繰り越し（…）」として出す

#### 呼ぶ回数そのものを減らす（2026-09-05。有料プランを避けるための対策）

上の予算方式は「上限に当たっても壊れない」ようにするものだが、それだけだと繰り越しが常態化して
返信検知が遅れていく。依頼元の意向（Workers Paid への移行は避けたい）を受けて、
**呼ぶ回数そのものを構造的に減らす**対策を追加した。

| 対策 | 削減効果（1巡回あたり） |
|---|---|
| **返信検知を「直近に動きのあったスレッド」だけに絞る**（`listActiveThreadIds`。`messages.list` を1回叩いてスレッドIDの集合を得る。本文は取らない） | **30回 → 1回＋実際に動きのあった数件（通常0〜3）** |
| 動きの無いタスクも毎回3件だけ見る背景スイープ（検索窓の外の見落としを拾う保険） | +3 |
| 処理済み・業務外判定済みのメールは本文取得より前にスキップ | 最大 −40 |
| カレンダー名→IDの解決結果を `settings.calendar_id_cache` に保存し `calendarList` を毎回呼ばない | −1 |
| 古いログ・判定記録の掃除を1日1回だけにする（`cleanup_done_on`） | −2 |
| 上限アラート・クレジット不足アラートの解除は、実際に立っているときだけ書き込む | −1〜2 |

決定打は1つめである。**返信検知はタスク1件につきスレッドを1回読む**設計だったため、
進行中タスクが増えるほど上限に近づいた。しかし実際には大半のスレッドは前回の巡回から何も
動いていない。`messages.list` の応答には各メッセージの `threadId` が含まれるので、
**1リクエストで「最近動きのあったスレッドID」の集合が手に入る**。あとはその集合に含まれる
タスクだけを読めばよい。

これで1巡回の使用数は**25回 → 15回前後**に下がり、進行中タスクが増えても
（動きのあった件数にしか比例しないので）ほぼ増えない。

#### 上限に当たる前に気づく

使用数が上限の8割を超えた日は、処理ログに赤い `error` で**1日1回だけ**警告を出す
（`settings.subrequest_warn_on`）。上限に当たってから気づくのでは、9/3と同じく
「記録処理ごと失敗して画面に何も残らない」状態になるため。

この警告が慢性的に出るようになったら、次の順で検討する:

1. 進行中（未処理・返信済み）のタスクを整理する（完了にする）。返信検知の対象が直接減る
2. `settings.reply_scan_days` を短くする（動きの判定窓を狭める）
3. `REPLY_CHECK_SWEEP_TASKS`（背景スイープの件数）を減らす
4. それでも足りなければ Workers Paid（上限1000）へ移行し `settings.subrequest_limit` を上げる

②を入れた理由は、①だけでは**同種の暴走を止めきれない**からである。①は「業務外メールの
再分類」という個別の穴を塞ぐが、原因Bの「ゲートが外れて実行回数が6倍になる」構造は残る。
別のバグで同じことが起きれば、また5分ごとにClaudeを呼び続けることになる。
①が塞ぐのは"何を送るか"、②が塞ぐのは"何回走るか"で、別の層である。

なお `last_fetch_at` 自体は**従来どおり最後まで完走したときだけ前進させる**。ここを
「途中でも前進させる」方向で直してはいけない。落ちた実行で窓を進めると、そのメールを
永久に取りこぼす（`worker/lib/anthropic.js` 冒頭の 2026-07-27 の事例がまさにこれ）。
取りこぼしを避けつつ暴走を止めるために、**窓（`last_fetch_at`）と実行間隔（`last_run_at`）を
別々の値に分けた**のが②の要点である。

### 対策の効果（実際の事故データで検証）

9/3の条件（cron `*/5`、稼働8〜18時JST、間隔30分、`last_fetch_at` が凍結）で
稼働時間中の実行回数を試算すると:

| | 実行回数 | 推定コスト | 凍結の検知 |
|---|---:|---:|---|
| 修正前 | 106回 | **$1.28** | 検知できず（丸一日気づけなかった） |
| 修正後（②④） | 18回 | $0.22 | **9/3 10:10 JST**（発生から約1.5時間後・業務時間内） |

修正前の試算$1.28が**実測$1.27とほぼ一致**しており、上記の原因分析（原因Bによる
実行回数6倍）が正しいことの裏付けにもなっている。

実際には①（`processed_messages`）が同時に効くため、この18回でも同じメールへの
Claude呼び出しは発生しない。上の$0.22は「①が別のバグで再び破られた場合」の
最悪値であり、②が単独でも被害を約1/6に抑えることを示している。

### 事故後に見つかった設定ミス（2026-09-04）

対策②の上限値 `settings.daily_api_cost_limit_usd` が本番で **`0.05`** になっていた
（既定 `0.50` の打ち間違いと思われる）。平常時の実績は **$0.06〜0.08/日** なので、
**この値では平常の利用でほぼ毎日サーキットブレーカーが作動し、メールの自動分類が
止まったままになる**。`0.5` に戻した。

歯止めそのものが業務を止める側に倒れていた、という点で見つけにくい類の設定ミスである。
設定画面の入力欄は下限・刻みとも `0.05` のため、矢印キーの操作だけでも到達してしまう。
再発防止として、**入力値が $0.10 を下回っている間は設定画面に赤い警告を出す**ようにした
（`LOW_LIMIT_WARN_USD`）。上限を下げること自体は禁止していない（意図して下げる場合が
あり得るため）が、平常運転を下回っていることは必ず画面に出る。

### 社長への説明に使う場合の補足

2章までの「年間約1,500円」という試算は**平常運転時の実績**であり、この事故を踏まえても
前提は変わらない（原因はバグであって利用量の増加ではない）。むしろ今回の対策により、
**「1日いくらまで」という上限を会社側が設定でき、超えたら自動的に止まり、通知で気づける**
状態になったため、予算化のしやすさは事故前より向上している。

---

## 10. Gemini（無料枠）への乗り換え再検討（2026-09-05。依頼元からの再検討依頼）

> 5章で一度「機密性の観点から非推奨」と結論したが、**社長が従量課金の発生自体に消極的**なため、
> より前向きに検討したいとの依頼を受けて、2026-09-05時点の情報で再調査した。
>
> **調査の制約（必ず読むこと）**: この作業環境からは `ai.google.dev`・`cloud.google.com` の
> 公式ページに到達できず（egress制限）、**一次情報を直接確認できていない**。以下の外部情報は
> Web検索の結果（第三者の解説記事）に基づく。モデル名・単価・無料枠の数値は変動が速いため、
> **実際に判断・発注する前に必ず公式ページで裏取りすること**。当システム側の数値（実測コスト・
> 呼び出し箇所・移行工数）は実データ・実コードに基づく確定情報である。

### 10-1. 結論（先に）

1. **無料枠は今も採用できない。** 2026年時点でも、Gemini APIの**無料枠は送信内容が
   Googleの製品改善（モデル学習）に利用され得る**という条件が続いている。有料枠に上げると
   この条件は外れる。当システムが送っているのは顧客の見積・請求・FAX（金額・数量・納期）や
   問い合わせ本文（会社名・氏名・連絡先）そのものなので、**5章の結論は変わらない**。
2. **ただし今回、新しい論点が1つある。** 「無料か有料か」ではなく「**有料でも桁が変わる**」。
   Gemini の Flash-Lite 系は Claude Haiku 4.5 より**1桁安い**ため、乗り換えれば
   年間約1,700円 → **年間約200〜600円**になる。前払い$5（約¥775）で**4〜8年分**に相当する。
3. **社長の懸念が「金額」なら②で解決する。「従量課金という契約形態」なら、乗り換えでは
   解決しない。** 後者を解決するのは9章で実装済みの三重の歯止め（前払い＋自動リチャージOFF＋
   アプリ側の1日上限で自動停止）であって、提供元を変えることではない。

### 10-2. 2026-09-05時点の外部情報（要裏取り）

| 項目 | 内容 |
|---|---|
| 無料枠のデータ利用 | **無料枠は入力・応答が製品改善に利用され得る／有料枠は利用されない**。この線引きは2026年時点でも継続 |
| 無料枠の縮小 | 2026-04-01にPro系が無料枠から除外され、**無料で使えるのは Flash / Flash-Lite 系のみ**になった |
| 無料枠の量 | Flash系でおおむね 10〜15 RPM・250,000 TPM・**1日250〜1,500リクエスト**（モデルにより差。当システムの実績は月200件程度なので量的にはまったく問題ない） |
| 有料の単価 | Flash-Lite 系で **$0.10〜0.30/100万入力トークン・$0.40〜2.50/100万出力トークン**（世代により差。現行 Claude Haiku 4.5 は $1.00 / $5.00） |
| 品質 | 文書読み取り・PDF理解でClaude Haiku 4.5と同等クラス。コンテキスト長は Gemini が有利（100万トークン）。**日本語のFAX・手書き帳票に限定した比較データは見つからなかった** |
| 変更の速さ | 2026-06-01に Gemini 2.0 Flash が廃止、2026-04-01に無料枠が縮小。**「無料枠は予告付きで縮む前提」で設計する必要がある** |

### 10-3. 当システムでの費用試算（実測ベース）

2026-07の実績（1ヶ月・187件・入力592,740／出力46,588トークン）を各単価に当てはめた:

| 提供元・モデル | 単価（入力/出力・100万トークン） | 月額 | 年額 | 備考 |
|---|---|---:|---:|---|
| **現行: Claude Haiku 4.5** | $1.00 / $5.00 | 約¥144 | **約¥1,730** | 前払い済み |
| Gemini Flash-Lite（安い世代） | $0.10 / $0.40 | 約¥14 | **約¥165** | 現行の約1/10 |
| Gemini Flash-Lite（新しい世代） | $0.30 / $2.50 | 約¥51 | **約¥615** | 現行の約1/3 |
| Gemini 無料枠 | 0 | ¥0 | **¥0** | **学習利用の条件付き＝採用不可** |

※ ¥155/$ 換算（`src/lib/pricing.js` の `USD_JPY`）。

**副次的な効果**: 単価が1桁下がると、**不具合で暴走したときの被害額も1桁下がる**。
9/3の事故（$1.27/日）は同条件で$0.13/日になる。9章の1日あたり上限と合わせると、
「最悪でも1日十数円」という説明ができるようになる。

### 10-4. メリット・デメリット

**メリット**

- **無料枠を使えば費用はゼロ**。当システムの利用量（月200件程度）は無料枠に対して十分小さい
- **有料枠でも1桁安い**。従量課金は残るが、金額の水準が「年間1,700円」から「年間200〜600円」になる
- 1リクエストあたりのコンテキスト長が大きく、長いスレッド・多ページPDFの扱いに余裕がある
- Googleアカウントで完結するため、Anthropicという**もう1社との取引関係を持たなくて済む**
  （社内の稟議・支払管理の観点では地味に効く。現在すでにGmail・Googleカレンダーを使っている）

**デメリット**

- **【決定的】無料枠は送信内容が学習に使われ得る。** 顧客の見積・請求・FAX・氏名・連絡先を
  外部の学習データに流す可能性を許容することになり、2026-07-15のセキュリティ審査で
  「anon/authenticated からは一切アクセス不可」まで厳格化した現在の方針（設計書8章）と正面から矛盾する。
  **万一これが顧客に知られた場合の説明責任は、年間1,700円の節約に見合わない**
- 無料枠は**レート制限（1分あたり・1日あたりの回数）に当たると止まる**。当システムの量なら
  通常は当たらないが、当たったときは「課金で解決できない＝待つしかない」。有料枠なら上限緩和で解決できる
- **無料枠の内容変更が速い**（2026年だけで4月に縮小・6月にモデル廃止）。予告なく条件が変われば
  そのたびに改修が必要で、**「無料のために保守工数を払う」構図**になりかねない
- **費用管理の仕組みが空回りする**。無料枠では「従量課金事項」画面の金額表示が常に¥0になり、
  9章で作った1日あたり上限（金額ベースのサーキットブレーカー）も機能しない。
  暴走の歯止めを**リクエスト回数ベースに作り直す**必要がある（＝追加の改修）
- プロンプトはClaude向けに1年かけて調整してきたもの（業務外判定・担当者振り分け・
  FAXの読み取り・ハルシネーション対策）で、**モデルを替えれば再調整と再検証が必要**。
  ここが移行の実質的なコストで、コードの書き換えより重い

### 10-5. 移行工数（当システム固有・実コードベース）

AI呼び出しは **`worker/lib/anthropic.js` の1ファイル・337行**に閉じており、公開関数は3つだけ:

| 関数 | 呼び出し元 | 用途 |
|---|---|---|
| `classifyEmail` | `worker/lib/pipeline.js` | メール・FAXの分類（添付PDF/画像の読み取りを含む） |
| `recognizeVehicle` | `worker/lib/reports.js` | 違反車両の写真からナンバー・車種を読み取る |
| `recognizeWasteSheet` | `worker/lib/waste.js` | 廃棄物実測値シートの読み取り |

したがって**この1ファイルを差し替えれば機械的な移行は済む**。付随して必要な作業:

1. リクエスト/レスポンス形式の変換（添付は `document`/`image` ブロック → `inline_data`）。
   なお Gemini は JSON出力のスキーマ指定に対応しており、`extractJson()` の手当ては**むしろ楽になる**
2. トークン数の取り出し（`usage.input_tokens` → `usageMetadata.promptTokenCount` 等）と、
   `src/lib/pricing.js` / `worker/lib/usageLimit.js` の単価差し替え（**2箇所で同じ値を保つ**）
3. 残高不足・レート制限の判定（`err.isBillingError` 相当）の作り直し。
   Gemini は429で返るため、**「一時的なレート制限」と「枠の使い切り」を区別する**必要がある
4. `GEMINI_API_KEY` を wrangler secret と GitHub Secrets に登録
5. **実メール・実FAXでの精度再検証**（ここが本番。過去のFAX・問い合わせで突き合わせる）

コード作業は**半日〜1日**、精度の再検証を含めると**2〜3日**が目安。

### 10-6. それでも無料でやるなら（現実的な折衷案）

- **廃棄物実測値シートだけをGemini無料枠に出す**という切り方はあり得る。あのシートは
  社内で計測した数値のみで顧客情報・個人情報を含まないため、学習利用の条件を飲める余地がある。
  ただし現状の利用量は月数件で、**節約額はほぼゼロ**。手間に見合わない
- **Cloudflare Workers AI の無料枠（1日10,000 Neurons・Workers無料プランでも利用可）**は、
  データが学習に使われず、追加の取引先も増えない点で無料枠としては筋がよい（4章）。
  ただし利用できるのはオープンモデル中心で、**日本語のFAX・手書き帳票の読み取り品質は
  Claude/Geminiに明確に劣る**という4章の評価は変わっていない。
  「通常メールの分類はWorkers AI、FAX・PDFはClaude」というハイブリッドなら成立し得るが、
  節約額（年1,000円程度）に対して実装・検証の手間が大きい

### 10-7. 推奨

**優先順位は次のとおり。**

1. **【第一推奨】現状維持（Claude Haiku 4.5・前払い・上限付き）。** 9章の対策により、
   前払い＋自動リチャージOFF＋1日あたり上限の三重の歯止めが入り、
   **「使い切ったら止まるだけで、追加請求は起きない」**状態になっている。
   社長の懸念が「青天井の請求」なら、これで実質的に解消している（2章・3章）
2. **【金額を下げたい場合】Gemini の有料枠（Flash-Lite）への乗り換え。** 従量課金である点は
   変わらないが、年間約1,700円 → 約200〜600円になり、事故時の被害額も1桁下がる。
   移行は2〜3日。**判断するなら、まず現行プロンプトで過去のFAX・メールを再分類させて
   精度を突き合わせる検証から始める**
3. **【採用不可】Gemini の無料枠。** 費用はゼロだが、顧客情報が学習に使われ得る条件は
   受け入れられない。加えて、金額ベースの安全装置が機能しなくなり、無料枠の条件変更に
   追随する保守が発生する

**社長への説明に使うなら**: 「無料のGeminiに替えれば0円ですが、その代わりに**お客様の見積書や
FAXの中身がGoogleのAIの学習に使われる可能性**があります。年間1,700円を節約するために
それを許容するかどうか、という選択です。もし金額を下げること自体が目的なら、
**同じGoogleでも有料版に切り替えれば年間200〜600円まで下がり、その場合は学習に使われません**」
という整理が、判断材料としていちばん誠実だと考える。

---

## 11. Gemini 有料枠へ切り替える場合の実装計画（2026-09-05。依頼）

> 10章の推奨②（**有料枠**への乗り換え）を実行する場合の計画。依頼元からの前提は2つ:
>
> - **A. Anthropic に戻す可能性が多分にあるため、現行ロジックを残したまま設定レベルで切り替えたい**
> - **B. これまでアプリ側ロジックでチューニングを重ねてきた判定（業務外・返信・担当割り当て・FAX）が
>   生かせるのか、組み直しになるのかを明確にしたい**
>
> 先に B から答える。**組み直しは発生しない。**

### 11-1. 結論: アプリ側ロジックは1行も組み直しにならない

コードを実際に確認した結果、**AI提供元に依存するのは `worker/lib/anthropic.js`（337行）だけ**で、
チューニングを重ねてきた判定ロジックはすべてその外側にある。

| チューニングしてきた判定 | 実装場所 | AIへの依存 | 切り替えの影響 |
|---|---|---|---|
| **返信判定**（件名ベース／スレッドベース／フォームのアドレス一致、時系列ガード、顧客発の返信でステータスを未処理へ戻す、counterpart特定、誤検知ガード） | `pipeline.js` | **AIを一切使わない**（純粋なJavaScript。設計書4-1手順6） | **影響ゼロ**。1行も変わらない |
| **FAX判定** | `pipeline.js` の `isFaxGatewayEmail()` | AIの `channel` より**アプリ側の判定を優先**する実装 | **影響ゼロ** |
| **業務外判定の扱い** | `pipeline.js`（FAXと實守紙業の数量報告は業務外判定でも必ずタスク化／`processed_messages` へ判定済み記録） | AIは `is_business_task` の真偽値を返すだけ | **影響ゼロ**。AIの答えの使い方は同じ |
| **担当者の割り当て** | `pipeline.js`（3名以外・空欄は「（担当未設定）」へ正規化、實守紙業は西川へフォールバック） | AIは3名から選ぶだけ | **影響ゼロ** |
| **振り分けルールそのもの** | **DBの `settings.org_context` / `business_keywords`**（画面から編集） | プロンプトに差し込まれる | **影響ゼロ**。設定値はそのまま使える |
| 取り込み順序・重複判定・時系列ガード・サーキットブレーカー・サブリクエスト予算・Web Push | `pipeline.js` | 無し | **影響ゼロ** |
| **プロンプト本文**（業務外の定義、担当者の決め方の優先順位、FAXの読み取り方、ハルシネーション対策、出力JSONの形式） | `anthropic.js` の `buildSystemPrompt()` ほか | — | **文面はそのまま移植できる**。ただし**同じ文面で同じ答えが返るかは要検証**（後述） |
| **APIの叩き方**（エンドポイント、添付の渡し方、トークン数の取り出し、リトライ、残高不足の判定） | `anthropic.js` | — | **ここだけ書き直し** |

規模で言うと、AIが絡む3ファイル（`pipeline.js` 1,474行＋`reports.js` 1,619行＋`waste.js` 322行）で
**AIを呼んでいるのは3行だけ**（`classifyEmail` / `recognizeVehicle` / `recognizeWasteSheet` の各1箇所）。

> **正確に言うと、組み直しになるのは「ロジック」ではなく「プロンプトの効き方」である。**
> 判定の手順・優先順位・例外処理はすべてアプリ側に書かれているので残る。一方、
> 「この文面を読んだAIが期待どおりに答えるか」はモデルごとに違うため、**プロンプトの再調整は
> 発生し得る**（文面を書き換える作業であって、ロジックの作り直しではない）。11-6の検証で洗い出す。

### 11-2. 切り替えの仕組み（前提Aへの回答）

**`settings.ai_provider`（`anthropic` / `gemini`。既定 `anthropic`）で切り替える。**
画面（従量課金事項）から変更でき、**デプロイ不要・即時反映・即時ロールバック可能**にする。

```
worker/lib/ai/
  index.js     … settings.ai_provider で提供元を選び、3つの関数を同じシグネチャで公開する
  prompts.js   … buildSystemPrompt など**プロンプト本文**（提供元に依存しない。現行の文面をそのまま移設）
  anthropic.js … 現行 worker/lib/anthropic.js の API 呼び出し部分（**中身は変えない**）
  gemini.js    … 新規。同じプロンプトを Gemini の形式で送り、同じ形の結果を返す
```

- `pipeline.js` / `reports.js` / `waste.js` の import 先を `./ai/index.js` に変えるだけ。
  呼び出し側のコードは変わらない
- **現行の Anthropic 実装は削除も改変もしない**（プロンプト本文を `prompts.js` へ移すだけ）。
  戻すときは設定値を `anthropic` に戻せば、今と完全に同じ経路になる
- APIキーは両方を登録したままにする（`ANTHROPIC_API_KEY` と `GEMINI_API_KEY`）。
  片方しか設定されていない提供元を選んだ場合は、起動時に赤いエラーログを出して**自動的に既定へ戻す**

### 11-3. Gemini 実装で作り込みが必要な差分

| 項目 | Claude（現行） | Gemini | 備考 |
|---|---|---|---|
| エンドポイント | `POST /v1/messages` | `POST /v1beta/models/{model}:generateContent` | ヘッダで API キーを渡す |
| システム指示 | `system` | `systemInstruction.parts[].text` | 文面は共通 |
| 添付（PDF/画像） | `document` / `image` ブロック＋`source.base64` | `inline_data`（`mime_type` + base64） | **現行の `collectClassifierDocuments()` が返す形式（パディング済み標準base64）をそのまま使える** |
| JSON出力 | プロンプトで指示＋`extractJson()` で救出 | `responseMimeType: "application/json"` ＋スキーマ指定が使える | **むしろ堅くなる**。`extractJson()` は保険として残す |
| トークン数 | `usage.input_tokens` / `output_tokens` | `usageMetadata.promptTokenCount` / `candidatesTokenCount` | **`thoughtsTokenCount`（思考トークン）は出力として課金される**（料金表の項目名が「出力料金（思考トークンを含む）」）。出力側に加算して計上する |
| 思考（thinking） | 使っていない | **既定で有効な場合がある** | **必ず最小／オフに明示設定する**（11-9参照。既定任せにすると出力トークンが数倍になり試算が崩れる） |
| リトライ | 403/408/429/5xx を最大3回 | 同様。ただし**429が「一時的なレート制限」と「枠の使い切り」の両方で返る** | 応答本文で区別する。区別できない場合は安全側（リトライせず記録）に倒す |
| 残高不足の検知 | 400 + `credit balance is too low` → `isBillingError` | 課金停止・請求先未設定は 403 / 429 で返る | 既存のクレジット不足バナー・Push通知の仕組みはそのまま流用できる |
| 添付の上限 | 1ファイル10MB・合計12MB・5件（アプリ側の制限） | インラインは要求全体で約20MBが上限 | 現行のアプリ側制限のままで収まる |

### 11-4. 費用計上（`api_usage`）の扱い — ここは設計変更が要る

現在は「トークン数 × Claudeの単価」で金額を**表示時に計算**しているため、
提供元が混在した月の金額が正しく出せない（9章のサーキットブレーカーも同じ問題を持つ）。

**対応方針: 加算時に金額を確定させて持つ。**

- `api_usage` と `api_usage_daily` に **`cost_usd numeric` 列を追加**し、RPC（`add_api_usage` /
  `add_api_usage_daily`）に `p_cost` を追加して加算する
- 金額の計算は Worker 側で、**そのとき使った提供元の単価**で行う
  （単価表は `worker/lib/ai/index.js` に提供元ごとに持ち、`src/lib/pricing.js` の表示用と値を合わせる）
- 画面・サーキットブレーカーは `cost_usd` を読むだけにする。既存のトークン列は内訳として残す
- あわせて `api_usage_daily` に `provider text` を持たせ、**どの提供元で何件処理したか**を残す
  （切り替え直後の切り分けに要る）

これは**切り替えの有無にかかわらず有用**なので、Phase 1 で先に入れてしまってよい。

### 11-5. 精度検証の計画（ここが本番）

**過去の実データで突き合わせる。** 本番の分類先をいきなり変えない。

**① オフライン再分類（必須）**

- 対象: 直近3ヶ月の `tasks`（メール由来・約200件）＋ **FAX由来は全件**＋
  `processed_messages` に残っている業務外判定分
- 方法: Gmail のメッセージIDから原文と添付を取り直し、Gemini で再分類して、
  保存済みのClaudeの結果（`assignee` / `channel` / `contact_company` / `contact_person` /
  `due_date` / `title` / `classification_note`）と機械的に突き合わせる
- **合否の基準（案）**

  | 指標 | 基準 |
  |---|---|
  | **業務メールを「業務外」と誤判定** | **0件**（1件でも出たら不合格。取りこぼしは顧客対応漏れに直結する） |
  | 業務外メールを「業務」と誤判定 | 許容（タスクが増えるだけ。現行も完全ではない） |
  | 担当者の一致率 | 90%以上 |
  | `channel`（email/form/fax）の一致率 | 100%（アプリ側で確定判定しているため、ずれたら実装ミス） |
  | FAXの `document_readable` | **読めない画像を「読めた」と答えない**こと（ハルシネーション対策が効いているか） |
  | 顧客名・担当者名の抽出 | 80%以上一致（残りは目視で質を確認） |

**② シャドー運用（任意・推奨）**

- `settings.ai_shadow_provider` を設けて、**本番の分類はClaudeのまま**、同じメールを
  Gemini にも投げて結果を処理ログに残すだけ、という期間を1〜2週間置く
- 実データで差分を見られる。Geminiは安いので追加費用はごくわずか
- **注意**: 1通あたりの外部リクエストが1回増える。9章のサブリクエスト予算に計上すること

**③ 手動機能の確認**

- 違反車両の写真読み取り（`recognizeVehicle`）と廃棄物シート読み取り（`recognizeWasteSheet`）は
  手動トリガーなので、**実際の写真数枚で目視確認**すれば足りる。
  特に廃棄物シートは**手書き数値**なので、読み取り精度の差が出やすい

### 11-6. 工程

| Phase | 内容 | 目安 | 本番への影響 |
|---|---|---|---|
| 0 | 公式ページでモデル・単価・利用規約の裏取り（10章の外部情報は未裏取り）、APIキー取得、支払い設定 | 0.5日 | 無し |
| 1 | プロバイダ抽象化のリファクタ（`worker/lib/ai/`）＋ `cost_usd` 列の追加。**この時点では Claude のみ・挙動は完全に同じ** | 0.5日 | 無し（挙動不変） |
| — | **→ Phase 1 は 2026-09-05 に完了・本番反映済み（11-10）** | — | — |
| 2 | Gemini 実装（`gemini.js`）＋ 設定画面の切り替えUI | 1日 | 無し（既定は anthropic） |
| 3 | オフライン再分類と突き合わせレポート | 1日 | 無し |
| 4 | （任意）シャドー運用 1〜2週間 | — | 分類結果は変わらない |
| 5 | 切り替え・監視 | 0.5日 | **ここで初めて変わる** |

**コード作業は実質2日、検証込みで3日。**シャドー運用を挟むなら2〜3週間の様子見期間が加わる。

Phase 1 だけを先に実施しておく判断もあり得る（**切り替えるかどうかを決めないまま、
いつでも切り替えられる状態にしておく**）。挙動が変わらないのでリスクがほぼ無い。

### 11-7. 撤退基準とロールバック

- **ロールバック手順**: 従量課金事項の画面で提供元を `Anthropic` に戻す。**デプロイ不要・即時**。
  Anthropic のクレジット残高は切り替え後も残るため、いつでも戻せる
- **即時撤退の条件**（1つでも該当したら戻す）
  1. 業務メールが「業務外」と判定されてタスク化されなかった事例が**1件でも**出た
  2. FAXの読み取りで、実在しない会社名・金額が生成された（ハルシネーション）
  3. 429（レート制限・枠超過）で分類が止まる事象が**2日連続**で起きた
- 切り替え後2週間は、**処理ログの「業務外」件数と新規タスク件数を毎日確認する**
  （急に業務外が増えていたら誤判定を疑う）

### 11-8. 未確定事項（判断前に確認が必要）

1. **モデルと単価の確定**（10章の外部情報は第三者記事に基づく未裏取り）。
   `gemini-2.5-flash-lite` 相当が最有力だが、世代交代が速いため**廃止予定日も併せて確認**すること
2. **有料枠でデータが学習に使われないことの明文**（利用規約の該当箇所を保存しておく。
   社内説明の根拠になる）
3. ~~**請求の上限設定**が Google Cloud 側でできるか~~
   → **解決（2026-09-05・実画面で確認）。Gemini API も「前払いクレジット＋オートチャージOFF」が選べ、
   残高が尽きたら止まるだけという Anthropic と同じ方式だった**。11-9 の訂正節を参照
4. 日本語の手書きFAX・帳票に対する読み取り精度（11-5の検証で実測する）

### 11-9. Gemini への確認結果と、判断への影響（2026-09-05）

11-8 の未確定事項について、依頼元が Gemini（チャット）に質問した回答を受領した。
**この節の「裏取り」列は、こちらで独立にWeb検索して確認できたかどうかを示す**
（公式ページには当環境から到達できないため、公式ページそのものの確認は依頼元のスクリーンショット待ち）。

| # | Geminiの回答 | 裏取り | 判断 |
|---|---|---|---|
| 1 | 課金は Cloud Billing に紐づく。AI Studio 単独の課金は無い | — | 妥当 |
| 2 | **AI Studio の画面から月間のハードキャップ（Spend cap）を設定でき、超過するとAPIが自動停止する** | **○ 取れた**（Project Spend Caps は2026-03-16に提供開始。加えて2026-04-01から請求アカウント単位のtier上限が必須化） | **最重要。11-8の最大の懸念が解消される見込み** |
| 2(a) | 予算アラートは通知のみ | ○ | 想定どおり |
| 2(b) | Cloud Function で請求を無効化する方法は今もあるが、プロジェクト全体が止まる | ○ | **採用しない**。Spend cap があるなら不要で、副作用が大きすぎる |
| 2(c) | Quotas を任意の値へ引き下げ可能。超過は 429「You exceeded your current quota」 | ○ | 回数ベースの二段目として使える |
| 3 | 有料枠のプロンプト・応答は学習に使われない（Additional Terms > Paid Services > How Google Uses Your Data） | 一致 | **規約本文のスクリーンショットを保存すること**（社内説明の根拠） |
| 4 | 無料/有料は**プロジェクト単位**で切り替わる。Billingを有効にした時点で配下の全キーが有料枠 | — | 運用上ありがたい（「気づかず無料枠のまま本番運用」が起きにくい） |
| 5 | 最安は `gemini-2.5-flash-lite`（$0.10/$0.40）。**廃止日は不明。2026-10-20という情報もある** | **△ 要注意**（Gemini APIの廃止一覧は「未定」、Agent Platform 側は 2026-10-20。Gemini 3 のGA後に確定日を設定し6か月前告知、との説明あり） | **新しいリスク。下記参照** |
| 6 | Gemini Developer API は**リージョン指定不可**（グローバル）。国内限定にするなら Agent Platform（旧Vertex AI）が必要 | — | **現行のAnthropic APIも同様**なので現状と同等。悪化ではない |

#### 判断への影響（2点）

**① 乗り換えの最大の障害は解消される見込み（好材料）**

Spend cap により、Anthropic の「前払い＋自動リチャージOFF」に相当する歯止めが再現できる。
移行後の安全装置は次の三重になり、**現行と同等以上**になる。

| 段 | 現行（Anthropic） | 移行後（Gemini有料枠） |
|---|---|---|
| 1段目 | 前払い残高＋自動リチャージOFF | **Spend cap（月額のハードキャップ）** |
| 2段目 | — | **Quota override（1日あたりのリクエスト数上限）** |
| 3段目 | アプリ側の1日あたり上限（9章） | 同じ（そのまま使える） |

ただし **Spend cap には約10分の反映遅延がある**（この間の超過は利用者負担）という情報がある。
上限額は「超えても許容できる額」で設定すること（例: 月$2＝約310円。平常の実績は月$0.09相当）。

**② 単価の前提は保守的に置き直すべき（悪材料）**

`gemini-2.5-flash-lite`（$0.10/$0.40）は**廃止時期が不透明**で、Agent Platform 側では
2026-10-20 という日付が出ている。ここに乗ると**数か月後にもう一度モデル移行が来る**恐れがある。
後継の Flash-Lite 世代は $0.30/$2.50 という情報があり、この単価だと:

| 前提 | 年額（実績ベース） | 現行比 |
|---|---:|---|
| 現行 Claude Haiku 4.5（$1.00/$5.00） | 約¥1,730 | — |
| `gemini-2.5-flash-lite`（$0.10/$0.40） | 約¥165 | 1/10 |
| **後継 Flash-Lite 世代（$0.30/$2.50）** | **約¥615** | **約1/3** |

**計画は後者（年約600円・現行の約1/3）を前提に立てること。** 1/10という数字で社内説明をすると、
モデル世代交代のたびに説明し直すことになる。年1,730円 → 年600円、**節約額は年1,100円程度**である。

#### 【重要な訂正】Gemini API も「前払いクレジット＋自動リチャージOFF」が選べる（2026-09-05・実画面で確認）

依頼元が AI Studio の **Dashboard → 使用量と請求 → 課金**（`Gemini APIの請求額`）の画面を確認した結果、
**11-8 の懸念（Anthropic の前払い＋自動リチャージOFF に相当する歯止めをどう再現するか）は、
そもそも課題ではなかった**ことが分かった。実画面に次の表示がある。

| 実画面の表示 | 意味 |
|---|---|
| **前払い(プリペイド)** ／ **オートチャージ: オフ** | **Anthropic とまったく同じ方式が選べる** |
| クレジット残高 ¥0 → 「サービスを再開するには、クレジット残高が0ドル以上必要です」 | **残高が尽きたらAPIが止まるだけ。追加請求は発生しない** |
| 「有料1・**$250 請求先アカウントの階層の上限**」 | 請求アカウント単位のtier上限（Tier 1 = $250/月）が実在する |
| 「クレジットは払い戻しできません」「有効期限は購入日から1年」 | **使い切らなかった分は1年で失効**。年600円の見込みなので $5 で約1年分 |

> **したがって、11-9 の当初の記述「Spend cap により Anthropic の前払いに相当する歯止めを
> 再現できる」は、より正確には「そもそも同じ前払い方式が使える」である。**
> Spend cap（月額の支出上限）は、前払い方式を選んだ場合は必須ではなく、二重の歯止めとして
> 任意に併用するもの、という位置づけになる。

移行後の安全装置は次のとおりで、**現行と完全に同形**になる。

| 段 | 現行（Anthropic） | 移行後（Gemini） |
|---|---|---|
| 1段目 | 前払い残高＋自動リチャージOFF | **同じ（前払いクレジット＋オートチャージOFF）** |
| 2段目 | — | Spend cap（月額）／Quota override（日次の回数）※任意 |
| 3段目 | アプリ側の1日あたり上限（9章） | 同じ（そのまま流用） |

**この確認により、「乗り換えると青天井に近づく」という懸念は解消した。**
残る判断材料は、費用（年約1,730円 → 約600円）と、モデル世代交代の頻度、精度の実測だけである。

#### Spend cap とレート制限の実画面（2026-09-05・依頼元が確認）

**AI Studio → Dashboard → 使用量と請求 → 利用額**（`Gemini APIの利用額`・Tier 1）:

| 実画面の表示 | 読み取れること |
|---|---|
| 「1か月の費用の上限」＋「費用の上限を設定」ボタン | **Spend cap は実在する** |
| **「試験運用版」ラベル** | **仕様変更・撤回があり得る。歯止めの本命にはしない**（本命は前払いクレジット＝正式機能） |
| 「レイテンシにより**10分程度の超過**が発生する可能性があります」 | 裏取りしていた10分の遅延が公式表記で確認できた |
| 「毎月1日（太平洋標準時）にリセットされます」 | 月次のリセット基準は**PST**（JSTの月初とはずれる） |
| **「費用が表示されるまでに数時間かかります。24時間以上かかることもあります」** | **Google側の画面ではリアルタイムに費用を追えない** |
| 「合計請求額には Gemini API の使用量のみが反映され、他の Google Cloud プロダクトの使用量は含まれない」 | Gemini API 単体の額が見られる |

> **③は移行後の設計にとって重要**。Google側の費用表示が最大24時間遅れるということは、
> **暴走の検知をGoogleの画面に頼れない**。9章で作った「アプリ側で1件ごとにトークンを数えて
> その場で日次上限を判定する」仕組みは、移行後もそのまま主役として必要になる
> （むしろ Anthropic のときより重要度が上がる）。

**AI Studio → レート制限**（Tier 1）: RPM 30 / TPM 200K。
当システムは30分に1回・1巡回あたり数件（1日十数件）なので**桁が違い、論点にならない**。
2(c) の Quota override も、前払い＋アプリ側上限で足りるため必須ではない。

#### モデルIDと単価の確定（2026-09-05・公式の料金ページで確認）

依頼元が Gemini API の公式料金ページを確認。**未確定だった単価とモデルIDが確定した。**

| モデルID | 入力（$/100万トークン） | 出力（**思考トークンを含む**） | ページの説明 | 無料枠 |
|---|---:|---:|---|---|
| `gemini-3.1-flash-lite` | **$0.25** | **$1.50** | 「最も費用対効果の高いモデル」 | あり（入出力とも無料） |
| `gemini-3.5-flash-lite` | **$0.30** | **$2.50** | 「最も費用対効果の高い**GA**モデル」 | あり（入出力とも無料） |
| （参考）`gemini-3.1-pro-preview` | $2.00〜4.00 | $12.00〜18.00 | プレビュー版 | 無し |
| （現行）`claude-haiku-4-5` | $1.00 | $5.00 | — | — |

**あわせて、全モデルの料金表に「プロダクトの改善に使用されます: 無料枠＝はい／有料枠＝いいえ」と
明記されていることを確認した。** 11-8 の未確定事項2（有料枠でデータが学習に使われない根拠）は、
規約本文より読みやすいこの料金ページのスクリーンショットで足りる。**社内説明にはこれを使うのがよい。**

**費用の再試算**（2026-07の実績: 入力592,740／出力46,588トークン。¥155/$）:

| | 月額 | 年額 | 現行比 |
|---|---:|---:|---|
| 現行 Claude Haiku 4.5 | 約¥128 | **約¥1,540** | — |
| `gemini-3.5-flash-lite` | 約¥46 | **約¥550** | 約1/3 |
| `gemini-3.1-flash-lite` | 約¥34 | **約¥405** | 約1/4 |

**節約額は年1,000〜1,100円。** 10-3 で「約1/3・年615円」と見積もった線とほぼ一致した
（10-3 の現行の月額¥144は累計トークンを月次と取り違えた数字。正しくは月¥128・年¥1,540）。

#### 【実装上の必須条件】思考トークンをオフにすること

**Gemini の出力課金には「思考（thinking）トークン」が含まれる**（料金表の項目名が
「出力料金（思考トークンを含む）」）。AI Studio の Run settings にも「Thinking level」の
設定があり、既定が High になっている場合がある。

現行の Claude Haiku 4.5 は思考なしで使っているため、**同じ条件で比較・運用するには
thinking を最小またはオフにする必要がある**。ここを忘れると:

- 出力トークンが数倍に膨らみ、上の試算が崩れる（思考が3倍出れば 3.5 Flash-Lite で年1,000円超。節約幅が半減）
- 9章の1日あたり上限（金額ベース）にも早く到達する

**`gemini.js` の実装では、思考の設定を明示的に最小へ倒すこと。** 既定値に任せない。

#### 残る確認事項（依頼元にお願いする作業）

1. ~~Spend cap・レート制限の実画面~~ → **すべて確認済み**（上記2節）
2. **Additional Terms の「Paid Services > How Google Uses Your Data」のスクリーンショット**
3. **`deprecations` ページ**で次の2点。ここが埋まれば Phase 0 は完了する
   - `gemini-3.1-flash-lite` / `gemini-3.5-flash-lite` の**廃止予定日**
   - **どちらが新しい世代か**。3.1 の方が安いのに 3.5 の方に「GAモデル」と書かれており、
     一方で 3.1 Pro は「プレビュー版」（＝3.1 の方が新しい世代に見える）。
     廃止日一覧を見れば世代の前後関係がはっきりする
   - ついでに `gemini-2.5-flash-lite`（$0.10/$0.40）が今も載っているかも確認できる
4. ~~Spend cap の反映遅延についての公式記述の有無~~ → **確認済み**（画面に「レイテンシにより10分程度の
   超過が発生する可能性があります」と明記）

**→ 残る未確定は 3（モデルID・単価・廃止日）のみ。** Documentation の `pricing` と `deprecations`
のページで確認できれば Phase 0 は完了する。

#### 実装への反映（11-3 の追補）

- 429 は「一時的なレート制限」と「Spend cap / Quota 到達」の**両方**で返り、本文も似ている。
  アプリ側は**429が短時間に連続したら、リトライせず停止してクレジット不足と同じ扱いにする**
  （バナー＋Push通知）方針にする。取りこぼしより「静かに回り続けて費用が出る」方が危険なため
- Spend cap は**月次**、アプリ側の上限は**日次**なので役割が重複しない。両方入れる

### 11-10. Phase 1 の実施記録（2026-09-05 完了・本番反映済み）

社長との調整が週明けになるため、**判断を待たずに入れられる Phase 1 だけを先行実施した**
（挙動が変わらないためリスクが無く、後で切り替えを決めたときに残りが1〜2日で済むようになる）。

**やったこと**

| 対象 | 内容 |
|---|---|
| `worker/lib/ai/prompts.js`（新） | 3つのシステムプロンプトを提供元非依存の場所へ集約。**文面は1文字も変えていない** |
| `worker/lib/ai/json.js`（新） | `extractJson()` を切り出し（提供元非依存） |
| `worker/lib/ai/anthropic.js` | `worker/lib/anthropic.js` から移設。**API呼び出しのロジックは一切変更なし**（差分はヘッダーコメントと2つのimport、`const system = …` の置き換えのみ） |
| `worker/lib/ai/pricing.js`（新） | 提供元→モデル→単価の表、`resolveProvider()`、`estimateCostUSD(provider, …)` |
| `worker/lib/ai/index.js`（新） | 切り替え口。`settings.ai_provider` で提供元を選ぶ。**未実装・未知の値は既定（anthropic）へ倒れる**ので設定ミスで業務は止まらない |
| `pipeline.js` / `reports.js` / `waste.js` | import 先を `./ai/index.js` に変更し、`ai_provider` を読んで渡すだけ。**判定ロジックには一切触れていない** |
| DB（`add_cost_usd_to_api_usage`） | `api_usage` / `api_usage_daily` に `cost_usd` を追加。RPC に `p_cost` を追加。**既存行は Claude 単価でバックフィル済み** |
| DB（`drop_obsolete_api_usage_overloads`） | 列を足すたびに積み上がっていた `add_api_usage` の旧オーバーロード3つを削除（`function is not unique` の潜在的な事故要因だった） |
| `usageLimit.js` | 金額を `cost_usd` から読むように変更。単価の重複定義を `ai/pricing.js` に一本化 |
| `src/lib/pricing.js` / `UsagePanel.jsx` | 画面の金額も記録済みの `cost_usd` を使う（`rowCostUSD()`。持っていない古い行だけ従来どおりトークンから試算） |

**確認したこと**

- **移設前後で3つのシステムプロンプトが1文字も違わないことを機械的に照合**（旧ファイルを
  git から取り出して文字列比較。classifyEmail 3,740文字 / recognizeVehicle 521文字 /
  recognizeWasteSheet 579文字がすべて完全一致）
- `rowCostUSD()` の4パターン（記録あり / 記録なし / 0 / 空行）が期待どおり
- 旧シグネチャ（`p_cost` 無し）での RPC 呼び出しも一意に解決できる
  ＝ **デプロイ前の旧Workerが動いている間も利用量の記録は壊れない**
- lint・build・`wrangler deploy --dry-run` を通過

**この時点でできるようになったこと**: `settings.ai_provider` を `gemini` にすれば提供元が切り替わる
（実装は Phase 2 で追加。現時点では既定へフォールバックするだけ）。**戻すときは設定値を
`anthropic` に戻すだけで、デプロイは不要。**

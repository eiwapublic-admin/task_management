# 備品管理機能 開発計画（蛍光ランプの入出庫・設置記録）

最終更新: 2026-08-12（**Phase 1 実装・main へのデプロイ・実機フィードバック3ラウンド＋
バナー色の再調整まで完了し、「ここまでOK」の回答済み**。以下は計画時からの経緯。
**確認事項18件すべてに回答をいただき反映済み**＝12章。
実装着手の前提が揃った状態。移行用CSV4本（全期間の入出庫830件）と帳票2点の実物を受領し、
全件を機械検証した結果を 8-1・8-4・8-5 に反映。**帳票（修理伝票PDF・修理データ）を範囲に追加**し、
owner にも修理伝票PDFを含めて閲覧を開放する形に変更した。テナント欄＝請求先という業務ルール
（3-4）は、過去データに例外があったため DB の CHECK 制約ではなくアプリ層のバリデーションに
変更した。新規入替の入力は共通項目＋備考のみで確定。**2026-08-12、新規入替の請求先・設置先が
入っていた1件（8-4(1)(2)）を運用側で解消した修正版CSVを受領**（今後の混入も心配ないことを確認）。
**同日、7-1（テナントマスタ取得）を Pull（FileMaker Data API）から Push（FileMaker→Worker）に変更**
（7-2 と同じ方式に統一。FileMaker Server の外部公開・Cloudflare Tunnel が不要になった）。
**さらに同期方式を「差分なしの全件洗い替え」に確定**（有効テナントは20件程度のため、FileMaker側で
差分計算をせず毎回全件送信。受信側は請求先コード一致でUpdate・不一致でInsert・未受信は論理削除
（`moved_out=true`）。→ 6-3・7-1）。**同日、Phase 1 の実装に着手**: DBスキーマ・Worker API
（`worker/lib/equipment.js`）・在庫一覧／履歴／備品・カテゴリマスタ画面／入庫・出庫（共用部設置・
不良品処分）モーダルを実装（テナント設置・新規入替・署名は Phase 2）。カテゴリ7件・備品22件は
実CSVから投入済み、入出庫は動作確認用に一部（200件・2019〜2021年分）を投入済み
（全件移行は別途 8-2 の手順で行う）。取込検証で新たな例外1件を発見（8-4(7)）。
main にマージしてデプロイし、**実機フィードバックの第1ラウンドを受けて即日反映**した:
①在庫一覧は備品名をタップしなくても明細がその場に展開表示される形に変更し、表示期間
（3ヶ月/1年/全期間）の切替も一覧画面の上部に統合した（5-1・5-2 を統合。旧・履歴専用画面
`EquipmentItemHistory.jsx` はURLとしては残すが、一覧からのリンクは廃止）。②入出庫モーダルの
`.ui-modal-body` に `overflow-x: hidden` を追加し、iOSでの横方向のラバーバンド揺れを防止した
（ui.css。プロジェクト共通部品として汎用化）。③**テナント設置・署名機能を前倒しで実装**
（本来 Phase 2 だが、UI/UXの確認をしたいとの要望を受けて着手）。テナント一覧APIとテナント
マスタ実データ52件を投入し、出庫・設置モーダルにテナント検索選択（`<datalist>` ＋デフォルト
備品自動反映）と署名パッド（`<canvas>` + Pointer Events。5-5の設計どおりゼロ依存で実装）を追加。
署名は「保存と同時」「後から追加」のどちらも `POST /api/equipment/signature`
（multipart: txn_id + file）の1本にまとめた（5-5では同時送信をmultipart一括のケースとして
書いていたが、実装では「まず記録を保存→署名があれば続けて登録」の2段階にして、後付けの口と
完全に共通化した。挙動は変わらない）。
**同日、実機フィードバックの第2ラウンドを受けて即日反映**した（iPad・iPhoneでの実機確認による）:
①**`<input list>` + `<datalist>` は Safari（iOS/iPadOS）で実質使い物にならないことが判明**
（候補が正しく出ない・キーボード上に数件だけ出る・iPhoneでは候補が出ない）。自前の候補リスト
を描画する `Combobox.jsx` に全面置き換えた（設置先テナント・設置場所・担当者・調達先）。
**⚠ 違反車両一覧（`ParkingViolations.jsx`）も同じ `<datalist>` 方式を使っており、同じ不具合を
抱えている可能性が高い（未確認・未修正・別タスク）**。②数量入力のネイティブスピンボタンを
CSSで非表示化（Mac Safariでの挙動不安定の指摘）。③受領サインの枠を強調（枠を太く・内側に
凹んだ影・ラベルを太字化）。④出庫理由の見出しを「設置先／出庫理由」に変え、ラベルを
テナント／共用部／不良品／新規入替の短縮表記に統一（iPhone幅で1行に収める）。**これに合わせて
新規入替（`reason='replace'`）も選択肢として解禁**（テナント設置と合わせて前倒し実装）。
⑤在庫一覧の明細表で、理由欄が1文字ずつ縦に折り返る不具合を修正（`white-space: nowrap` ＋
横スクロール化）。日付表示は時刻を省き年月日のみに変更。横スクロール時も日付列だけは
`position: sticky` で固定表示されるようにした。
**同日、実機フィードバックの第3ラウンドを受けて即日反映**した:
①在庫一覧の入出庫明細表の日付表示に曜日を追加（`yyyy/mm/dd(曜)`。`formatEquipmentDate`
がja-JPロケールに`weekday:'short'`を足すだけで自動的にこの表記になることを確認して採用）。
②明細表の列順を「日付・設置先・出庫・入庫・残・理由・担当」に変更（従来は理由が2列目・
設置先が6列目だったのを入れ替え、出庫/入庫の列順も入れ替えた）。③在庫一覧の出庫／入庫
ボタンを両方 `.btn-primary`（青）にし、表示順も出庫→入庫に統一（「1画面に主要ボタンは1つ」
というUI標準の原則からは外れるが、両方とも同格の主要操作であるという実機での指摘を優先）。
④備品名バナー（`.equipment-stock-row.is-static`）の背景を `--bg-subtle` から `--bg-muted`
（見出し帯用の濃いめの背景）に変更し、備品の切り替わりが視認しやすいようにした
（**この直後にさらに2段階の修正が入っている。末尾の追記を参照**）。
⑤ハンバーガーメニューのドロップダウンが、カンバンの担当者選択行・日報一覧の年月行の
下に隠れる不具合を修正。原因は `.dashboard-header`（1段目ヘッダー）と `.ui-sticky-head`・
`.ui-sticky-head-2`（2段目・3段目の固定ヘッダー。カンバンの担当者選択行や日報一覧の年月行が
これに該当）が同じ `--z-header` を使っており、DOM順で後にある2段目側が同値の重なり順勝負に
勝ってしまっていたため。`--z-header-2`（1段下）を新設して2段目・3段目側だけそちらに変更し、
1段目ヘッダー（ドロップダウンを含む）が常に上に来るようにした。⑥候補リスト付き入力欄
（`Combobox.jsx`）の右端に×のクリアボタンを追加（一度入力すると候補が絞り込まれて選び直し
にくくなるため、ワンタップで空にして候補を出し直せるようにした）。⑦画面全体のピンチ拡大を
`viewport` メタタグで禁止（`maximum-scale=1.0, user-scalable=no`）。意図せず一覧画面が拡大
されたまま気づかず、レイアウトがはみ出して見えるとの指摘のため。写真・PDFの拡大表示
（`AttachmentPreview.jsx`）は独自のJS実装（pinch/wheelでの`scale()`変形）でこの設定と無関係。
**続けて④備品名バナーの背景色だけ2段階の再調整が入った**（実機での見え方確認、いずれも即日）。
まずカンバンの「完了」ステータス色そのもの（`--status-done` = `#16a34a`）を塗ったところ「彩度が
高くてけばしい」との指摘。使ってほしかったのはステータスの点そのものの色ではなく、**カンバンの
「完了」列が実際に使っている淡い背景色**だったため、`.kanban-column` と同じ
`color-mix(in srgb, var(--status-done) 12%, var(--bg-muted))` に変更（`KanbanBoard.css` の式を
そのまま流用）。明るさが `--bg-muted` の頃とほぼ同じ淡さに戻るため文字色の上書きは撤回。
「ここまでOK」の回答を得て、備品在庫一覧の実機フィードバックはここで一区切りとなった。
**次のアクションは実機での最終確認（バナー色を含む）、その後帳票・Web Push・FileMaker連携
（Phase 2以降の残り）への着手判断**）

対象: 現行 FileMaker アプリ **「備品管理」**（オンプレ FileMaker Server `eiwaserver.eiwa-up.com` 稼働）の
Web（Cloudflare Workers + Supabase）への移行。既存のタスク管理・日報と**同じアプリ内**に
第3のセクションとして統合する。

関連文書:
- [`task-management-spec-cloudflare.md`](./task-management-spec-cloudflare.md) … システム全体の設計書（実装後に4-15章として追記する）
- [`daily-report-plan.md`](./daily-report-plan.md) … 日報機能の計画。**移行の進め方・確定事項の考え方はこの文書に倣う**
- [`ui-standard.md`](./ui-standard.md) … **画面設計はこの標準に準拠する**（本計画の画面案もこの標準の部品名で書いている）
- [`HANDOFF.md`](./HANDOFF.md) … 運用・デプロイ手順

> 補足: 日報の計画（`daily-report-plan.md` 8章）では「備品入出庫は**保留**（現時点では移行対象外）」と
> していたが、2026-08-11 に**移行対象として着手**することになった。日報の Phase 6 に含まれていた
> 「FileMaker 連携（蛍光灯ランプの交換実績）」は、本機能で**双方向**（テナントマスタの取得／設置実績の提供）
> として実現するため、この計画に統合する。

---

## 1. 目的と範囲

### 目的

1. 蛍光ランプ（および電球・LED・電池）の**入庫と出庫（設置）を記録し、在庫を管理する**
2. **テナントへの設置**と**共用部への設置**を区分して記録する
3. テナント設置時は、**iPad / スマホに手書き署名**をもらい、受領の証跡として保存する
   （その場でもらうのが基本だが、**先に記録だけ入力して後日署名だけもらう**運用もあるため、
   システム上は必須にしない。2026-08-11 確定。5-5 参照）
4. テナントマスタは FileMaker Server 上の**検針記録データベースを正**とし、Web 側は同期して使う
5. FileMaker 側のアプリから、**テナントに設置したランプ実績を年月指定で WebAPI 取得**できるようにする

### 今回の範囲に含むもの

| 現行 FileMaker の画面 | 移行方針 |
|---|---|
| 入出庫記録（一覧・在庫表示） | 移行する。**モバイル前提に作り直す**（後述 5-1） |
| 入庫登録 | 移行する（5-2） |
| 出庫・設置記録（テナント／共用部） | 移行する。署名込み（5-3・5-4） |
| 備品情報（備品マスタ） | 移行する（5-5） |
| カテゴリマスタ | 移行する。件数が少ないため備品マスタ画面内のモーダルで扱う（5-5） |
| テナントマスタ | **参照専用**として移行。FileMaker から同期し、当システムで持つのは「デフォルト備品」だけ（5-6・7-1） |

**各種出力（帳票）も範囲に含める（2026-08-11 に実物を受領し確定。8-5）**:

| 出力 | 内容 |
|---|---|
| **修理伝票（印刷用PDF）** | テナント設置1件＝1枚（A4に2枚）。**受領印欄に手書き署名画像が入る**＝署名機能の出口 |
| **修理データ（請求連携用の表）** | 月次・テナント別・備品別の設置本数の集計。現行は Excel を書き出して請求へ回している |

### 今回の範囲に**含めない**もの（合意が要る／優先度が低い）

- **ログオン**（現行の独自ログオン）… 当システムの既存ログイン（JWT）に統一する
- 発注機能（在庫警告は出すが、発注そのものは行わない）

---

## 2. 現行 FileMaker アプリの分析（画面ショットから）

移行の元になる情報。**ここが実際の運用と食い違っていないかの確認を最初にお願いしたい**（12章）。

### 2-1. 入出庫記録（メイン画面）

- ヘッダ: `入出庫 / 備品 / カテゴリ / テナント / 各種出力 / ログオン`、`3ヶ月前から表示する`、`修正ロック`、
  右上に `入庫` `出庫` ボタン、注記「**＊在庫調整は「入庫」から**」
- 備品ごとの見出し帯: 備品名（FLR40SW）・製品番号（FLR40SW/M/36）・**在庫数（44）**・入庫/出庫ボタン。
  在庫が警告数量以下だと**赤地**になり「**発注依頼してください。**」が出る
- 年ごとの小見出し: **年計（入庫 52 / 出庫 8）**
- 明細行: `入出庫日付 / 備品 / 入庫 / 出庫 / 在庫（その行時点の残高） / 階 / 設置先 / 署名（サムネイル） / 担当`
  - 入庫は緑字、出庫は青字。**在庫調整は入庫にマイナス値**（例: `-2`）で記録されている
  - 在庫がマイナスの行も存在する（`-2`）＝ **在庫のマイナスは禁止されていない**
  - 設置先は `喫煙室(1F)` `通路(1F)` `備後町富士法律経済研究所(7F)` のように**共用部の場所名とテナント名が同じ列**に出る
  - 担当は `信国` `廣畑・岡田` `西川` のように**1レコードに複数名が入ることがある**（自由入力欄）

### 2-2. 入庫登録

`入出庫区分=入庫` / 入庫日付 / 入庫時刻 / **入庫理由（調達・繰延登録・在庫調整）** / 備品ID（ドロップダウン） /
調達先 / 入庫数量 / **入庫前在庫数・入庫後在庫（自動計算の表示）** / 担当者 / 備考（在庫調整理由など）

### 2-3. 出庫・設置記録

`入出庫区分=出庫` / 入出庫日付 / 入出庫時刻 / **入出庫理由（テナント設置・共用部設置・新規入替・不良品処分）**

- **テナント設置**: `設置先テナント`（ドロップダウン＋右に補足欄） / 備品ID / 出庫数量 / **在庫数 44 → 44** /
  担当者（「担当者を選択」） / **受領サイン**（枠・「iPadで入力可」）＋`受領サイン`ボタン＋`取消`ボタン /
  **受領タイムスタンプ** / 備考 / 修正者・修正情報タイムスタンプ
- **共用部設置**: テナント欄の代わりに `設置階`（階のドロップダウン）＋`設置場所`（「設置場所を選択」）

画面下部に**一般ユーザの制約**が明記されている（＝そのまま権限仕様として移行する。9章）:

> ・出庫の場合、受領サインが入力されるとデータの修正ができません。
> ・入庫の入力はできますが、後から修正することはできません。
> ・前日以前に入力した入出庫は削除できません。

### 2-4. 備品情報（備品マスタ）

カテゴリごとにグルーピングして表示。列は
`備品ID / 表示順 / 備品名 / 製品番号（発注用情報） / 警告数量 / 無効 / 在庫数`。右上に「備品追加」。

- 備品ID は 2, 3, 5, 42, 47, 51, 52, 53 … と**連番でない整数**（＝FileMaker の既存 ID。移行の突合キーになる）
- 表示順は 11, 12, 13, 14, 99 … （99＝末尾）
- `無効` にチェックが付いた備品（FL20SSW）は在庫数欄がグレーアウト
- 「**予備40型（在庫管理外）**」という備品があり、在庫が黄色の `-2`。**在庫管理の対象外**という区分がある

### 2-5. カテゴリマスタ

`カテゴリコード / カテゴリ名称 / 備考` の3列・7件。
`A2 蛍光灯 20` `A4 蛍光灯 40` `A6 蛍光灯 丸型` `A9 その他蛍光灯` `B1 電球` `C1 LED` `D1 電池`

### 2-6. テナントマスタ

注記に「**※検針記録データベースを直接参照しています。ここでは「デフォルト備品」のみ設定します。**」とある。

列は `階 / 請求先コード / 正式名称 / 退去済み / A20_テナント備品関連::デフォルト備品ID / 検針に関する備考`。

- **共用部もテナントマスタに1行ある**（請求先コード `99990001` = `共用エリア`、階は空）
- 請求先コードは8桁（`73050011` など）。同一テナントで**光熱用と家賃で請求先コードが別**の例あり（三越伊勢丹）
- デフォルト備品ID は当システム側で持つ値（＝FileMaker 備品管理アプリ固有。検針記録DB側には無い）

---

## 3. 全体方針

### 3-1. 既存の作りに合わせる（新規性を持ち込まない）

日報機能を追加したときと**同じ形**で足す。これにより既存の運用・デプロイ手順・セキュリティ設計を
そのまま引き継げる。

| 層 | 方針 |
|---|---|
| フロント | `src/pages/Equipment*.jsx` を追加。**`docs/ui-standard.md` に準拠**（`.ui-page` → `.ui-container` → `.ui-toolbar`） |
| ヘッダ | `[ タスク ｜ 日報 ｜ 備品 ]` の3セグメントに拡張（`AppHeader.jsx`） |
| API | `worker/lib/equipment.js` を新設し、そこに閉じて実装する（`reports.js` と同じ流儀）。既存コードには手を入れない |
| DB | Supabase に新テーブル群。`anon`/`authenticated` への GRANT は**なし**（service role のみ）＝既存方針どおり |
| 認証 | 既存の JWT。書き込みは `canWrite()`（owner 不可）。加えて備品固有の修正ロック（9章） |
| 画像 | 署名画像は Supabase Storage。既存バケット `report-photos` に `equipment/signatures/` プレフィクスで置く |

### 3-2. 在庫数は「持たない」。入出庫の積み上げで算出する

在庫数を列として持つと、入出庫の修正・削除のたびに整合を取る必要があり、ズレたときに原因を追えない。
**入出庫レコードが唯一の正**とし、在庫はビューで集計する（4-2）。

- 現在庫 = Σ(入庫) − Σ(出庫)
- 明細行の「在庫」（その行時点の残高）は、**現在庫から新しい行の増減を順に引き戻して**算出する
  （日付降順に並べた一覧で、追加のクエリを増やさずに出せる）
- **在庫のマイナスは許容する**（現行がそうなっている）。保存時に警告は出すが、ブロックはしない

### 3-3. FileMaker とは当面「並行運用」する

日報のときと同じく、いきなり止めない。テナントマスタの同期（7-1）と設置実績の提供 API（7-2）が
入った時点で、現行 FileMaker アプリ側は**参照だけ**にできる状態を目指す。停止の判断は運用を見てから。

### 3-4. テナント欄は「場所」ではなく「請求先」である（2026-08-11 確認。最重要）

> 「新規入替の設置先は備考欄に書く。**テナント名を入れるとそこに向けての請求を意味する。
> 新規入替は請求対象外**」（確認事項③'）

設計上、これは単なる入力項目の有無の話ではない。**`tenant_code` が入っているレコードは
「そのテナントに請求する」という意味を持つ**ため、次を全レイヤーで守る。

| レイヤー | 守り方 |
|---|---|
| 画面（5-4） | テナント選択欄は**テナント設置のときだけ**出す。新規入替では設置先を**備考の自由記述**として書く |
| API（`worker/lib/equipment.js`） | 保存時に `reason≠'tenant'` なら `tenant_id`/`tenant_code`/`tenant_name` を無視・拒否する（**アプリ層で強制**。2026-08-11 に DB の CHECK 制約からこちらへ変更。4-1） |
| 外部API（6-2） | 請求の元データになるため、**新規入替・不良品処分は一切返さない**。`scope=tenant` は `reason='tenant'` のみ |
| 移行（8-4） | 取込時に新規入替の請求先・設置階・設置先は取り込まず、備考のみ残す（**2026-08-11 確定**。確認事項⑬⑭） |

**なぜ守るか**: 誤ってテナントを選んだまま新規入替で保存すると、**本来請求しないものを
請求してしまう**（＝金銭的な影響が出る）。画面の出し分けだけに頼ると、後から入力区分を変えたとき
などに値が残る事故が起こりうるため、**画面とAPIの2層**で止める（DBのCHECK制約は、過去データの
例外（8-4(1)(2)）を機械的に一律拒否してしまうため、あえて持たないことにした）。

---

## 4. データモデル（Supabase）

`supabase/schema.sql` に追記し、同じ SQL をマイグレーションとして適用する（既存手順どおり）。

### 4-1. テーブル

```sql
-- ============================================================
-- 備品管理（2026-08-xx〜）。現行 FileMaker「備品管理」アプリの移行。
-- ============================================================

-- カテゴリマスタ（A2 蛍光灯 20 / A4 蛍光灯 40 / …）
create table if not exists equipment_categories (
  code       text primary key,                 -- A2 / A4 / A6 / A9 / B1 / C1 / D1
  name       text not null,
  sort_order int  not null default 99,
  note       text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- 備品マスタ
create table if not exists equipment_items (
  id            uuid primary key default gen_random_uuid(),
  -- FileMaker の「備品ID」（2, 3, 5, 42, 53…）。CSV移行・外部APIの突合キーなので必ず保持する
  item_no       int  not null unique,
  category_code text references equipment_categories(code),
  name          text not null,                 -- FLR40SW (白色)
  product_code  text,                          -- FLR40SW/M/36（発注用情報）
  sort_order    int  not null default 99,
  warn_qty      int,                           -- 警告数量。在庫がこの値以下で「発注依頼してください」
                                               -- Web Push のしきい値も兼ねる（10-1）。null なら通知しない
  -- 在庫警告を最後に通知した日時。「下回っていない→下回った」に変化した時だけ鳴らすために持つ。
  -- 在庫が warn_qty を上回ったら null に戻す（入庫で解消したら次回また鳴らせる）
  warned_at     timestamptz,
  disabled      boolean not null default false, -- 無効（選択肢に出さない。過去の記録は残す）
  -- 「予備40型（在庫管理外）」のように在庫を管理しない備品。一覧では在庫欄を出さない
  track_stock   boolean not null default true,
  note          text,
  created_at    timestamptz not null default now(),
  updated_at    timestamptz not null default now()
);
create index if not exists equipment_items_category_idx
  on equipment_items (category_code, sort_order, item_no);

-- テナント（正は FileMaker の検針記録DB。当システムは同期したコピーを持つ）
create table if not exists equipment_tenants (
  -- 主キーは uuid（サロゲート）。当初は billing_code を主キーにしていたが、実データに
  -- 請求先コードが空のテナントが2件あった（どちらも退去済み）ため、NULL を許せる形にした（8-4(3)）
  id              uuid primary key default gen_random_uuid(),
  -- 請求先コード（8桁。99990001 = 共用エリア）。NULL 可・あれば一意
  billing_code    text unique,
  name            text not null,               -- 正式名称（修理伝票に出すのはこちら＋「様」）
  short_name      text,                        -- 略称（入出庫の設置先・請求データの表記はこちら。8-1）
  floor           text,                        -- 階（共用エリアは空）
  -- 退去済み。2026-08-12〜: FileMaker からの全件洗い替え同期（6-3・7-1）で、送信された
  -- 有効テナント一覧に含まれなくなった行は自動でこれが true になる（論理削除。物理削除はしない）
  moved_out       boolean not null default false,
  -- ここだけ当システム側の設定値。同期で上書きしない
  default_item_id uuid references equipment_items(id),
  note            text,                        -- 検針に関する備考（同期対象）
  source          text not null default 'filemaker',  -- filemaker / manual
  synced_at       timestamptz,                 -- 最後に FileMaker から同期を受信した日時
  created_at      timestamptz not null default now(),
  updated_at      timestamptz not null default now()
);
create index if not exists equipment_tenants_floor_idx on equipment_tenants (floor, billing_code);

-- 入出庫（このテーブルが在庫の唯一の正）
create sequence if not exists equipment_txn_no_seq;

create table if not exists equipment_transactions (
  id          uuid primary key default gen_random_uuid(),
  -- 問い合わせ・調査で特定しやすい短い連番（tasks.task_no と同じ考え方。画面上は「E-123」）
  txn_no      bigint not null unique default nextval('equipment_txn_no_seq'),
  item_id     uuid not null references equipment_items(id),
  kind        text not null check (kind in ('in', 'out')),
  -- in : procure=調達 / deferred=繰延登録 / adjust=在庫調整
  -- out: tenant=テナント設置 / common=共用部設置 / replace=新規入替 / discard=不良品処分
  reason      text not null check (reason in
                ('procure','deferred','adjust','tenant','common','replace','discard')),
  occurred_at timestamptz not null,            -- 入出庫日時（日付＋時刻）
  -- 入庫は正（在庫調整のみ負を許す）／出庫は正。0 は許さない
  quantity    int not null check (quantity <> 0),
  supplier    text,                            -- 調達先（入庫）※実データでは未使用（8-4(5)）
  -- テナント設置＝請求対象。この欄が入っている＝そのテナントへ請求する、という意味を持つ（3-4）
  tenant_id   uuid references equipment_tenants(id),
  -- 名称・コードは記録時点のスナップショットも持つ（テナントの改称・退去後も帳票と請求履歴が崩れない）
  tenant_code text,
  tenant_name text,                            -- 正式名称（修理伝票用）
  tenant_short_name text,                      -- 略称（請求データ用。現行の表記に合わせる）
  -- 階・場所は「どこに設置したか」であって請求とは無関係。共用部設置に加え、
  -- 新規入替でも実際に使われている（8-4(2)）ため、reason では縛らない
  floor       text,                            -- 設置階（テナント設置時はテナントの階を写す）
  location    text,                            -- 設置場所（喫煙室 / 通路 / 玄関ホール …）
  staff_name  text,                            -- 担当者（複数名は「廣畑・岡田」のように1欄）
  signature_key text,                          -- 署名画像の Storage キー（テナント設置のみ）
  -- 署名を受け取った時刻。移行データは FileMaker の受領タイムスタンプをそのまま入れる。
  -- 「signed_at あり・signature_key なし」＝署名済みだが画像は現行 FileMaker 側（8-4(4)）
  signed_at   timestamptz,
  -- 移行元 FileMaker の「入出荷ID」。再取込の冪等性と、問い合わせ時の突合のために持つ
  legacy_txn_id int unique,
  note        text,
  created_by  uuid references users(id),
  updated_by  uuid references users(id),
  created_at  timestamptz not null default now(),
  updated_at  timestamptz not null default now(),
  -- 区分と理由の整合（入庫の理由で出庫を登録する等の取り違えを DB 側でも止める）
  constraint equipment_txn_kind_reason check (
    (kind = 'in'  and reason in ('procure','deferred','adjust')) or
    (kind = 'out' and reason in ('tenant','common','replace','discard'))
  ),
  -- 負数は在庫調整のときだけ
  constraint equipment_txn_negative_only_adjust check (quantity > 0 or reason = 'adjust'),
  -- 署名はテナント設置のときだけ持つ
  constraint equipment_txn_signature_only_tenant check (signature_key is null or reason = 'tenant')
  -- 「請求先（tenant_id等）を持てるのはテナント設置のときだけ」（3-4）は、あえて DB の CHECK
  -- 制約にはしない。過去データに例外が実在した（8-4(1)(2)）ことに加え、この業務ルールは
  -- 「新規登録の画面でテナントを選ばせない」という入力制御が本質であり、DB が一律拒否すると
  -- 移行のたびに手当てが要る。2026-08-11 の回答（確認事項⑬⑭）により、
  -- worker/lib/equipment.js の保存時バリデーション（アプリ層）でのみ強制することに変更した
);
create index if not exists equipment_txn_item_idx     on equipment_transactions (item_id, occurred_at desc);
create index if not exists equipment_txn_occurred_idx on equipment_transactions (occurred_at desc);
create index if not exists equipment_txn_tenant_idx   on equipment_transactions (tenant_code, occurred_at desc);

drop trigger if exists equipment_transactions_set_updated_at on equipment_transactions;
create trigger equipment_transactions_set_updated_at before update on equipment_transactions
  for each row execute function set_updated_at();

-- 既存方針どおり anon/authenticated からは一切触れない（Worker の service role 経由のみ）
alter table equipment_categories   enable row level security;
alter table equipment_items        enable row level security;
alter table equipment_tenants      enable row level security;
alter table equipment_transactions enable row level security;
revoke all on equipment_categories, equipment_items, equipment_tenants, equipment_transactions
  from anon, authenticated;
```

### 4-2. 集計ビュー

PostgREST から `group by` 相当が使えないため、集計はビューで用意する（既存の日報機能では使っていない
手法だが、ここは行数が増える一方の入出庫を毎回全件取って JS で集計するより素直で速い）。

```sql
-- 現在庫
create or replace view equipment_stock as
  select i.id as item_id,
         coalesce(sum(case when t.kind = 'in' then t.quantity else -t.quantity end), 0)::int as stock_qty,
         max(t.occurred_at) as last_moved_at
    from equipment_items i
    left join equipment_transactions t on t.item_id = i.id
   group by i.id;

-- 年計（画面の「年計： 52  8」）。年は JST 基準
create or replace view equipment_yearly_totals as
  select item_id,
         extract(year from (occurred_at at time zone 'Asia/Tokyo'))::int as year,
         sum(case when kind = 'in'  then quantity else 0 end)::int as in_qty,
         sum(case when kind = 'out' then quantity else 0 end)::int as out_qty
    from equipment_transactions
   group by 1, 2;

revoke all on equipment_stock, equipment_yearly_totals from anon, authenticated;
```

> **注意（過去の教訓の適用）**: 関数・ビューを後から作り替えるときは、`create or replace` だけでは
> 権限が既定（PUBLIC）に戻ることがある。`api_usage` の `add_api_usage()` で実際に踏んだ問題なので
> （HANDOFF「経緯の要約」31番）、**作成・変更のたびに `revoke` を必ずセットで流す**。

### 4-3. 署名画像の保管

- 既存バケット **`report-photos`**（非公開）を再利用し、キーは `equipment/signatures/<txn_id>.png`
- 新バケットを作らない理由: バケット追加は `worker/lib/storage.js` の改修（バケット名の引数化）と
  従量課金画面の使用量集計の見直しを伴う一方、署名 PNG は 1 枚 10〜30KB 程度で、
  年間数百枚でも数MB。分ける実益が薄い
- 取得は既存の写真と同じく **JWT 認証を通った Worker 経由のみ**（`GET /api/equipment/signature?txn_id=…`）

---

## 5. 画面設計

**すべて `docs/ui-standard.md` に準拠する。** 具体的には:
骨格は `.ui-page` → `.ui-container` → `.ui-toolbar`、緑＝現在地／青＝操作、`.btn-primary` は1画面1つ、
アイコンのみのボタンは `title` + `aria-label`、削除は `ConfirmDeleteButton`、モーダルは `.ui-overlay` /
`.ui-modal` + `useBodyScrollLock()`、高さは `max-height: 100%`（`vh`/`dvh` を使わない）、
CSS に生値を書かない。

### 5-0. ヘッダのセクション切替

`[ タスク ｜ 日報 ]` を `[ タスク ｜ 日報 ｜ 備品 ]` にする（`AppHeader.jsx`）。

- 現在の `inReports` 真偽判定を `section = 'tasks' | 'reports' | 'equipment'` の3値に変える。
  デスクトップ用・モバイル用に**同じ nav が2箇所ある**ので両方直す
- ハンバーガーメニューの中身も section で出し分ける（備品セクションでは「在庫一覧」「備品マスタ」
  「テナント一覧」「従量課金事項」）
- **owner（小泉産業様）にも出す**（2026-08-11 変更。確認事項⑦）。日報と同じ**閲覧のみ**。
  ハンバーガーの項目も owner には閲覧できるものだけを出す（マスタ編集は出さない）
- ⚠ **要検証**: iPhone 幅（375px）でヘッダは既に2行に折り返しており、セグメントが3つになると
  2行目が窮屈になる可能性がある。`.app-switch .ui-segmented-btn` の左右パディングは狭幅で既に
  詰めてあるので、収まらなければラベル短縮ではなくパディング/フォントサイズのトークン変更で対応する
  （実機幅での確認を「出す前のチェックリスト」に含める）

### 5-1. 在庫一覧 `/equipment`（このセクションのホーム）

現行の「入出庫記録」に相当するが、**現行のような1画面全部入りの密なリストにはしない。**
現行は 27 インチの Mac 画面が前提で、iPhone ではそのままでは使えないため、
**「在庫を見る」と「履歴を見る」を分ける**。

```
[ 在庫 ]                                        [ 入庫 ] [ 出庫 ]
  ┌────────────────────────────────────────┐
  │ A4 蛍光灯 40                            │  ← カテゴリ見出し
  │  FLR40SW (白色)      FLR40SW/M/36    44 │  ← 行タップで履歴へ
  │  FLR40EX (昼白色)    FLR40S EX-N/M/36 0 │  ← 赤バッジ＋「発注依頼」
  └────────────────────────────────────────┘
```

- ツールバー: 左＝タイトル「備品」、右＝`入庫`（`.btn-plain`）と `出庫`（`.btn-primary`。現場で一番使う操作）
- 行は**カテゴリごとにグルーピング**（`.ui-sticky-head-2` でカテゴリ見出しを固定表示）
- 在庫が `warn_qty` 以下なら `.ui-badge.is-danger` ＋「発注依頼してください」（現行と同じ文言）
- `disabled` の備品は既定で非表示（トグルで表示）。`track_stock=false` は在庫欄を「—」にする
- **横スクロールする要素の中で `.ui-sticky-head` 系を使わない**
  （`sticky-header-overflow-trap` スキル参照。過去に3回踏んだ罠）

### 5-2. 備品の履歴 `/equipment/items/:itemNo`

行タップで開く。現行の明細部分に相当。

- 見出し: 備品名・製品番号・**現在庫**・年計（入庫/出庫）
- 期間フィルタ: `3ヶ月 / 1年 / 全期間`（既定 3ヶ月。現行の「3ヶ月前から表示する」に相当）
- 明細: `日付 / 入庫 / 出庫 / 残 / 設置先 / 担当 / 署名アイコン`。行タップで詳細モーダル（＝修正）
- 入庫は緑字・出庫は青字（現行の色分けを踏襲）。ただし**トークンの色**を使う（生値を書かない）
- **署名列は3状態**（署名あり＝サムネイル／**署名待ち＝バッジ**／対象外＝空欄）。テナント設置なのに
  未署名の行は「署名待ち」を出し、そこから直接署名モーダルを開ける（5-5。署名を必須にしない代わりの安全網）

### 5-3. 入庫モーダル

現行の「入庫登録」。`.ui-modal.is-sm`。

| 項目 | UI |
|---|---|
| 入庫日時 | `datetime-local`（既定＝現在。残留塩素の測定日時と同じ作り） |
| 入庫理由 | 択一ボタン `調達 / 繰延登録 / 在庫調整`（残留塩素の「測定施設」と同じ形。セグメントは使わない※） |
| 備品 | `.ui-select`（カテゴリ見出し付き `<optgroup>`。`disabled` は出さない） |
| 調達先 | `.ui-input` ＋ 過去値の候補（`Combobox.jsx`。→ 下の注記） |
| 入庫数量 | 数値。**在庫調整のときだけマイナスを許可**する |
| 在庫の変化 | `44 → 94` と自動表示（現行の「入庫前在庫数／入庫後在庫」） |
| 担当者 | `.ui-input` ＋ 過去値の候補（`Combobox.jsx`。後述 5-7） |

> **【2026-08-12 変更】`<input list>` + `<datalist>` は使わない。** 当初の想定どおり実装したが、
> 実機（iPad・iPhoneのSafari）で候補が正しく表示されない・キーボードの上に数件だけ出る・
> iPhoneでは候補自体が出ないという重大な不具合が判明した。**自前の候補リストを描画する
> `Combobox.jsx`（`src/components/`）に置き換えた**（ui.css に `.ui-combobox` を追加）。
> ⚠ 違反車両一覧（地域・メーカー・車種・テナント名）も同じ `<datalist>` 方式のため、
> **同じ不具合を抱えている可能性が高い**（未確認・未修正。HANDOFF参照）。
| 備考 | `.ui-textarea`（プレースホルダ「在庫調整理由など」） |

※ `.ui-segmented` は「同じ対象の見せ方の切替」に使う部品で、**データの入力値には使わない**
（`ui-standard.md` 3章）。理由の選択は択一ボタン（`.btn-plain.is-active`）で表現する。

### 5-4. 出庫・設置モーダル

現行の「出庫・設置記録」。理由によって入力項目が切り替わる。
**理由のラベルは4つとも短縮表記（テナント／共用部／不良品／新規入替）にしている**
（2026-08-12。「テナント設置」等の5文字だとiPhone幅で択一ボタンが1行に収まらず、
かつ明細一覧の理由欄では1文字ずつ縦に折り返って表示が崩れたため。見出しも
「出庫理由」から**「設置先／出庫理由」**に変えて、この欄が設置先も兼ねることを示す）。

| 項目 | テナント | 共用部 | 新規入替 | 不良品 |
|---|---|---|---|---|
| 設置先テナント | ○（必須） | — | **—** | — |
| 設置階・設置場所 | 自動（テナントの階） | ○（必須） | **—** | — |
| 備品・数量・担当者・備考 | ○ | ○ | ○ | ○ |
| **受領サイン** | **○（任意）** | — | — | — |

> **新規入替は「共通項目＋備考」だけ（2026-08-11 確定。確認事項②・③'）。** 当初は「テナント設置と
> 同じ項目」と仮置きしていたが、実際の運用では**備考しか入力しない**。**2026-08-12、選択肢として
> 解禁**（テナント設置と同じタイミングで実装。バックエンドの `SUPPORTED_REASONS` に追加）。
>
> **設置先はテナント欄ではなく備考に書く。** これは入力の手間の話ではなく、
> **テナント欄に名前を入れる＝そのテナントへ請求する、という意味になる**ため（3-4）。
> 新規入替は請求対象外なので、テナント欄を出してはいけない。DB の制約でも縛っている（4-1）。

- **テナント選択**: 件数が数十件あるため、`.ui-select` ではなく**検索できる選択 UI**にする。
  当初は `<input list>` + `<datalist>` を想定していたが、**実機での不具合により `Combobox.jsx`
  に変更した**（上の 5-3 の注記を参照）。`退去済み` は既定で候補から外す。
  選択するとテナントの**階**と**デフォルト備品**を自動で埋める（デフォルト備品はテナントマスタの
  設定値。現行の `A20_テナント備品関連::デフォルト備品ID` と同じ役割）
- **設置場所（共用部）**: 過去に使った場所の候補（`Combobox.jsx`。使ったことがある場所ならすぐ選べる）＋自由入力
- **在庫の変化**: `44 → 42` を自動表示。**0 を下回る場合は赤字で警告するが保存はできる**（3-2）
- 保存は `.btn-primary`「記録する」。フッタは左＝削除（`ConfirmDeleteButton`）／右＝キャンセル・記録する
- **数量入力のスピンボタン（上下矢印）は非表示にする**（2026-08-12。Mac Safari 等でクリック時の
  挙動が不安定に見えるとの指摘を受け、`.ui-input[type=number]` に `appearance: none` 系のCSSを適用）

### 5-5. 署名モーダル（テナント設置）

**この機能の肝。** iPad / スマホで指・Apple Pencil で手書きしてもらう。

- `.ui-modal` の上に重ねる入れ子モーダル（`--z-modal-nested`）。`useBodyScrollLock()` は
  入れ子でも数を数えるので、内側を閉じても外側のロックは外れない（`ui-standard.md` 3章）
- **外部ライブラリは使わない。** `<canvas>` + Pointer Events（`pointerdown/move/up`）で実装する。
  この規模なら数十行で済み、依存を増やす方がリスクが大きい（既存の方針＝ゼロ依存を踏襲）
- 実装上の注意点（先に潰しておく）:
  1. `touch-action: none` を canvas に指定する。付けないと**書いている最中に画面がスクロール**する
  2. `devicePixelRatio` を掛けた実サイズで canvas を作り、CSS サイズと分けて持つ（Retina でぼやけない）
  3. 線は `lineCap/lineJoin = 'round'`、`lineWidth` は 2〜3 の固定（筆圧は使わない）
  4. `pointercancel` / `pointerleave` でもストロークを閉じる（iOS で指が枠外に出たとき）
  5. 保存時に**描画範囲を切り出して余白を詰め**、長辺 600px 程度に縮小して `canvas.toBlob('image/png')`。
     背景は透過ではなく**白**で塗る（帳票・PDF に載せたときに黒背景に潜らないように）
  6. `取消`（消して書き直し）と `閉じる` を分ける
  7. 横向き（landscape）でも枠が潰れないよう、高さは `max-height: 100%` の枠内で比率固定にする

#### 署名は必須にしない。「あとから署名だけ足す」流れを正面から作る（2026-08-11 確定。確認事項④）

> 「テナントの署名は必ずしてもらえるが、システム上は必須にしなくてよい。
> **先に入力しておいて後から署名だけもらうこともある**」との回答による。

これは制約をゆるめるだけの話ではなく、**運用フローが1本増える**ため設計に反映する。

- **保存フローを2通りにする**:
  1. その場で署名 → 署名 PNG を入出庫レコードの保存と**同時**に送る（multipart。当初の想定どおり）
  2. **先に記録だけ保存 → 後日、署名だけ足す** → 既存レコードに署名を後付けする専用の口を用意する
     （`POST /api/equipment/transactions/:id/signature`）。`signed_at` には**署名を受け取った時刻**を入れる
     （記録の入力時刻とは別物なので、`created_at` とは必ず分けて持つ）
- **未署名が埋もれないようにする**: 一覧・履歴で**「署名待ち」が一目で分かる**ようにする
  （署名列にバッジを出す＋在庫一覧のツールバーに「署名待ちのみ」の絞り込み）。
  必須をやめる以上、これが無いと「もらい忘れ」に気づけない。**必須化の代わりに置く安全網**という位置づけ
- **署名を足す導線**: 一覧の該当行 → 履歴（5-2）または編集モーダルから「署名をもらう」ボタン →
  この署名モーダルを開く。iPad を持って現地でテナントを回る使い方を想定し、**記録を開き直さなくても
  署名だけ入れて閉じられる**ようにする
- **修正ロックとの関係**（9章）: 「署名済みは staff は修正不可」は現行どおり維持する。
  署名が後から付く運用なので、**未署名の間は staff も直せる**（現行 FileMaker と同じ挙動）。
  署名を足した時点でロックがかかる、という順序になる
- ⚠ **実機確認が必須**。過去、モーダル・写真まわりは Chromium では再現しない iOS 固有の不具合を
  何度も踏んでいる（HANDOFF 40・56〜60・112番）。**iPad（Pencil / 指）と iPhone の両方**で確認する

### 5-6. 備品マスタ `/equipment/items`

- カテゴリごとにグルーピングした一覧。列は現行と同じ（備品ID / 表示順 / 備品名 / 製品番号 / 警告数量 /
  無効 / 在庫数）。行タップで編集モーダル、ツールバー右に `備品を追加`
- **カテゴリマスタ**は件数が7件と少ないため専用画面を作らず、この画面のツールバーから開くモーダルで
  一覧・追加・編集する

### 5-7. テナント一覧 `/equipment/tenants`

- FileMaker から同期した内容の**参照**（階・請求先コード・正式名称・退去済み・備考）
- 当システムで編集できるのは **デフォルト備品** だけ（現行の注記と同じ）
- ツールバーに `同期`ボタンと「最終同期: 2026-08-11 04:00」の表示。同期は 7-1

### 5-8. 担当者の選択（要件「既存データから選択、または、新規入力」）

過去の入出庫の `staff_name` から**重複を除いた候補**を返す API を用意し、
`<input list>` + `<datalist>` で「選ぶことも、新しく打つこともできる」形にする。
違反車両の地域・メーカー・車種で既に使っている方式（`daily-report-plan.md` Phase 4）と同じで、
利用者にとっても操作が揃う。

> タスク管理の `settings.assignees`（担当者名リスト）とは**別扱いにする**。備品の設置担当は
> 協力会社を含む現場の作業者で、タスクの割り当て先とは母集団が違うため。
> **2026-08-11 に「過去の設置担当の Distinct」で確定**（確認事項⑧）。

---

## 6. API 設計（Worker）

`worker/lib/equipment.js` に新設し、`worker/index.js` の `route()` に分岐を足す。
既存と同じく **JWT 必須・書き込みは owner 不可**。外部連携用の 6-2 だけが例外。

### 6-1. 社内向け（JWT）

| メソッド/パス | 内容 |
|---|---|
| GET `/api/equipment/items` | 備品マスタ＋現在庫＋年計。`include_disabled=1` で無効も含む。在庫一覧・各モーダルの選択肢の元 |
| POST/PATCH/DELETE `/api/equipment/items` | 備品の追加・変更・削除（入出庫が1件でもあれば削除不可。`disabled` を促す） |
| GET/PUT `/api/equipment/categories` | カテゴリの一覧・丸ごと差し替え（定型文と同じ流儀） |
| GET `/api/equipment/transactions` | 入出庫の一覧。`item_no=` / `from=` / `to=` / `limit=`。各行に**その時点の残高**を付けて返す |
| POST `/api/equipment/transactions` | 入出庫の登録。`multipart/form-data`（署名 PNG を同送**できる**。署名なしでも登録可）。`kind`/`reason` の整合はサーバー側でも検証 |
| PATCH `/api/equipment/transactions` | 修正（9章の修正ロックをサーバー側で判定） |
| DELETE `/api/equipment/transactions?id=…` | 削除（同上。署名画像の実体も消す） |
| **POST `/api/equipment/transactions/:id/signature`** | **既存レコードへの署名の後付け**（5-5。「先に入力して後から署名だけもらう」運用のため）。未署名のレコードにのみ許可し、成功したら `signed_at` を立てる＝以後 staff は修正不可になる |
| GET `/api/equipment/signature?txn_id=…` | 署名画像の本体（非公開バケットなのでこの経路のみ） |
| GET `/api/equipment/suggest?field=staff\|location\|supplier` | 過去値の候補（`<datalist>` 用。重複除去・使用頻度順） |
| GET `/api/equipment/tenants` | テナント一覧（`include_moved_out=1` で退去済みも。`synced_at` を含み「最終同期」表示に使う） |
| PATCH `/api/equipment/tenants` | `default_item_id` の設定（同期対象外の当システム独自項目）や、`billing_code` 空欄テナントの手動修正 |

> `POST /api/equipment/tenants/sync` は**JWT ではなく APIキー認証**（FileMaker からの Push）。
> 6-3 参照。当初は「画面の同期ボタンで手動実行」という JWT 経路も検討していたが、
> 7-1 の Push 化（2026-08-12）に伴い、テナント同期は FileMaker 側が起点の一本化にした。

### 6-2. FileMaker 向けの公開 API（APIキー認証）

**要件「FileMaker のアプリから、テナントに設置したランプ情報を年月指定で取得」への回答。**

```
GET /api/equipment/installations?month=2026-08[&scope=tenant|common|all][&tenant_code=…]
Header: X-API-Key: <EQUIPMENT_API_KEY>
```

> ⚠ **この API は請求の元データになる**（テナント欄＝請求先。3-4）。返す対象を次に限定する。
>
> | scope | 返す `reason` |
> |---|---|
> | `tenant`（既定） | `tenant` のみ |
> | `common` | `common` のみ |
> | `all` | `tenant` + `common` |
>
> **`replace`（新規入替）と `discard`（不良品処分）はどの scope でも返さない。**
> 新規入替は請求対象外であり、ここに混ぜると請求されてはいけないものが請求されうるため。

```json
{
  "month": "2026-08",
  "generated_at": "2026-09-01T00:05:00+09:00",
  "count": 2,
  "installations": [
    {
      "txn_no": 1234,
      "installed_at": "2026-08-10T10:22:00+09:00",
      "tenant_code": "73050011",
      "tenant_name": "静岡県大阪事務所",
      "floor": "1",
      "item_no": 2,
      "item_name": "FLR40SW (白色)",
      "product_code": "FLR40SW/M/36",
      "quantity": 2,
      "staff_name": "西川",
      "signed": true,
      "note": null
    }
  ]
}
```

セキュリティ設計（**これは JWT の外側に開く唯一の口なので厚めに**）:

1. `X-API-Key` を Worker シークレット `EQUIPMENT_API_KEY` と**タイミング安全比較**（長さ比較＋
   全バイト XOR。早期 return しない）
2. **送信元 IP の許可リスト**（`EQUIPMENT_API_ALLOW_IPS`。`CF-Connecting-IP` で判定）。
   FileMaker Server は固定の自社回線から出るため、ここは実際に効く防御になる。
   未設定なら IP 制限なし（＝キーのみ）で動くようにし、設定を必須にはしない
3. **読み取り専用**。書き込み系のパスは一切生やさない
4. KV（`LOGIN_ATTEMPTS` と同じ仕組み）で**レート制限**（例: 同一 IP 60回/時）
5. 返す情報は**テナント名・階・備品・数量・担当者名まで**。署名画像そのものは返さない（`signed` の
   真偽のみ）。個人情報を最小化する既存方針に沿う
6. 呼び出しは `activity_logs` に1行残す（呼び出し頻度は月数回想定なので溢れない）
7. キーの生成・ローテーション手順を HANDOFF に記載する（GitHub Secrets → Deploy 実行）

FileMaker 側の呼び出し例（`Insert from URL`、cURL オプション）:

```
"-X GET
--header " & Quote("X-API-Key: ***") & "
--header " & Quote("Accept: application/json")
```

> **なぜ JWT ではなく API キーか**: JWT は人のログインに紐づく30日期限のトークンで、
> 機械間連携に使うと期限切れで静かに止まる。用途を分けたほうが運用が破綻しない。

### 6-3. FileMaker からのテナント同期受信（APIキー認証・書き込み専用）← 【2026-08-12 新設／08-12 全件洗い替え方式に確定】

7-1 が Push 方式になったことに伴う受け口。**6-2 とは別キー**（`EQUIPMENT_TENANT_SYNC_API_KEY`）で、
6-2 の「読み取り専用」原則を崩さないよう完全に切り離す。

**毎回「現在有効なテナント全件」を送る、差分なしの全件洗い替え方式**（7-1 参照。有効テナントは
20件程度のため、差分計算はせずFileMaker側の負担を減らす）。

```
POST /api/equipment/tenants/sync
Header: X-API-Key: <EQUIPMENT_TENANT_SYNC_API_KEY>
Body: [
  { "billing_code": "...", "name": "...", "short_name": "...", "floor": "...", "note": "..." },
  ...  ← 現在有効なテナントの配列（退去済みは含めない）
]
```

**受信側の処理（1リクエスト＝1トランザクションで実行）**:

1. 送られてきた `billing_code` が**既存と一致** → その行を **Update**（`name`/`short_name`/`floor`/`note`
   を上書き。`moved_out` が立っていれば **false に戻す**＝復帰扱い。`default_item_id` は当システム
   独自項目のため上書きしない）
2. 送られてきた `billing_code` が**既存に無い** → **Insert**（`source='filemaker'`、`synced_at=now()`）
3. **既存の `source='filemaker'` 行のうち、今回のリクエストに含まれなかったもの** → **論理削除**
   （物理 DELETE はしない。`moved_out=true` を立てるだけ。過去の入出庫が参照しているため）

セキュリティ・安全設計:

1. **6-2 とは独立したキー**で、6-2 のキーではこのエンドポイントを叩けない
2. `DELETE` は一切生やさない（3 の論理削除も内部的には UPDATE）
3. 送信元 IP の許可リスト（`EQUIPMENT_API_ALLOW_IPS` を共用）、KV でのレート制限（6-2 と同じ仕組み）
4. 呼び出しは `activity_logs` に1行残す（送信件数・Insert/Update/論理削除の件数を記録）
5. **空配列や極端に少ない件数を拒否するガード**を入れる（例: 0件、または前回成功時の件数から
   大きく減った場合はエラーにして 3 を実行しない）。**全件洗い替え方式は「送られなかった＝退去」と
   みなす設計**のため、FileMaker 側のスクリプト不具合や通信の途中失敗で不完全な配列が届くと、
   有効なテナントを誤って一括で論理削除してしまう事故になりうる。ここが唯一のリスクなので、
   件数チェックだけは省略しない

FileMaker 側の呼び出し例（`Insert from URL`）:

```
"-X POST
--header " & Quote("X-API-Key: ***") & "
--header " & Quote("Content-Type: application/json") & "
--data " & Quote(JSON配列)
```

---

## 7. FileMaker 連携

### 7-1. テナントマスタの取得（Push・FileMaker → Worker）← 【2026-08-12 方式変更】

要件「テナントマスタは FileMaker Server 上のものから FileMaker API で取得したい」への回答。
**当初は Pull（Cloudflare Worker → FileMaker Data API）で計画していたが、2026-08-12 に Push
（FileMaker 側からの送信）へ変更した。**「テナント情報変更の頻度が少なく、トリガーは FileMaker 側が
持った方が送信処理の無駄がない」との判断による。これにより 7-2（設置実績の提供）と方向が揃い、
FileMaker↔Worker 間の通信は**常に FileMaker が Worker を呼ぶ一方向**に統一される。

**方式**: FileMaker Server → Cloudflare Worker（6-2 と同じ APIキー認証の、書き込み専用エンドポイントを新設）。

```
POST https://<worker-host>/api/equipment/tenants/sync
  Header: X-API-Key: ***（テナント同期専用。6-2 の読み取り専用キーとは別に発行）
  Body: 現在有効なテナントの配列（請求先コード・正式名称・略称・階・備考。退去済みは含めない）
```

**同期の作り（2026-08-12、全件洗い替え方式に確定）**:
- **差分計算はしない。トリガーが発火するたびに「現在有効なテナント全件」をまとめて送る**。
  「差分の判断が面倒で、有効件数が20件程度しかないため」との判断による（個別レコード単位の
  Push・差分Pushは採用しない）
- **トリガーは FileMaker 側のレイアウト保存時スクリプト**（テナントの追加・改称などでレコードが
  保存されるたびに、その時点の有効テナント全件を検索し直して配列で送信）
- Worker 側は `billing_code` を突合キーに **一致=Update／不一致=Insert**。
  さらに**既存の FileMaker 由来テナントのうち、今回の配列に含まれなかったもの＝退去とみなし
  論理削除**（`moved_out=true`。物理削除はしない。過去の入出庫が参照しているため）（→ 6-3）
- 一度 `moved_out=true` になった請求先コードが**再び配列に含まれれば自動で復帰**（`moved_out=false`）
- `default_item_id` は当システム側でのみ持つ項目のため、送信内容で**上書きしない**
- **請求先コードが空のテナントはこの仕組みの対象外**（8-1 で判明した過去の2件はどちらも退去済みで
  移行時に別途処理済み。FileMaker 側の運用では請求先コードが必ず入る前提）
- **全件洗い替えゆえの唯一のリスクは「不完全な配列が届いて有効テナントを誤って一括退去にしてしまう」
  こと**。Worker 側で空配列・極端な件数減少を拒否するガードを入れて防ぐ（→ 6-3）
- **FileMaker が落ちていても現場の入力は止まらない**。当システムは受信済みのコピーで動く。
  送信失敗は Push の性質上 Worker 側からは検知できないため、テナント一覧画面の「最終同期」表示
  （`synced_at`）が実質的な監視になる

**必要な準備（栄和側／FileMaker 側の作業）**:

| # | 内容 | 誰が |
|---|---|---|
| 1 | テナントレイアウトに**保存時スクリプトトリガー**を設定し、**有効テナント全件を検索して配列で送信**するスクリプトを作成（差分計算は不要） | FileMaker 管理者 |
| 2 | 当方が払い出す送信用 APIキーを、スクリプトの `cURL` オプションに設定 | 両者 |

Pull 方式（旧案）で最大の論点だった「FileMaker Server の外部到達性・正式な証明書」は、
**Push では不要になった。** FileMaker Server がインターネットから到達可能である必要はなく、
外向きに `Insert from URL` を発行できればよい（7-2 と同条件）ため、**Cloudflare Tunnel の導入も
不要**になる。なお、ユーザーより「FileMaker Server は既にHTTPSで外部公開済み」との補足があったが、
Push 化によりこの構成の有無に依存しなくなった（公開していても・Tunnel の裏に隠しても、いずれでも
Push は成立する）。

**Worker シークレット（追加）**: `EQUIPMENT_TENANT_SYNC_API_KEY`（6-2 の読み取り専用キーとは別に
発行する、テナント同期専用の書き込みキー）

### 7-2. 設置実績の提供（Push 方向・6-2 の API）

FileMaker 側から `Insert from URL` で 6-2 を叩く。**当システム側の作業だけで完結**し、
FileMaker Server を公開する必要もない。**7-1 も同じ Push 方式になったため、7-1・7-2 とも
先行して着手できる**（旧案にあった「7-1 は FileMaker 側の到達性整備を待つ」という制約は無くなった）。

---

## 8. 初期データの移行と各種出力（FileMaker からの CSV／帳票）

> 8-1〜8-4 が移行、**8-5 が帳票（修理伝票PDF・修理データ）**。どちらも 2026-08-11 に
> 実ファイルをいただいて確定したものなので、同じ章にまとめている。

要件「初期データは FileMaker からダウンロードした CSV を引き渡す」への回答。

### 8-1. 受領した CSV（4本。2026-08-11 受領・検証済み）

**実ファイルを受領し、全件を機械的に検証した。** 形式は UTF-8・**CR 改行**（FileMaker/旧Mac 形式。
取込スクリプトは `\r` を改行として扱う必要がある）。ファイル名は macOS の NFD 正規化
（「デ」＝「テ」＋濁点の合成）なので、スクリプト側で NFC に正規化してから突合する。

| ファイル | 件数 | 実際の列 |
|---|---|---|
| カテゴリマスタ | 7 | カテゴリコード / カテゴリ名称 / 備考 |
| 備品マスタ | 22 | 備品ID / 備品名 / 備品補足 / 製品番号 / カテゴリコード / メーカー / 警告数量 / 表示順 / 備考 / **備品名_補足** / 入庫累計 / 出庫累計 / 在庫数 / flg_在庫警告 / 最終入出庫ID / flg_無効 / 主キー / 作成・修正情報 |
| テナントマスタ | 52 | 階 / 請求先コード / 正式名称 / **略称** / 退去済み / 検針に関する備考 |
| 入出庫データ | **830**（2026-08-11 に全期間で再受領・**2026-08-12 に新規入替1件の請求先・設置先を消去した修正版を再受領**。旧30件版は抜粋だったと判明） | 入出荷ID / 入出庫区分 / 入出庫日付 / 入出庫時刻 / 入出庫理由 / 調達先 / **設置先請求先コード** / 設置階 / 設置先 / 備品ID / 入庫数量 / 出庫数量 / 担当者 / 備考 / **受領タイムスタンプ** / 主キー / 作成・修正情報（ほか集計用の `c_*` 列多数） |

**使う列 / 捨てる列**（「冗長に持っているものもあるので必要な項目を利用して下さい」との指示による）:

- 備品マスタの `在庫数` `入庫累計` `出庫累計` `flg_在庫警告` `最終入出庫ID` は**取り込まない**。
  在庫は入出庫の積み上げで出す方針（3-2）のため、持つと二重管理になる。**取込後に、CSV の在庫数と
  こちらの計算結果が一致するかの検算にだけ使う**
- 入出庫の `c_*`（`c_入出庫後在庫` 等）も同様に**検算専用**で取り込まない
- 備品マスタの `備品名_補足`（例: `FLR40SW (白色)`）は `備品名` + `備品補足` の連結。**帳票の「型番」欄が
  この表記**（8-4）なので、表示用として使う（DB は `name` と `note`/補足を分けて持ち、表示時に組み立てる）
- テナントマスタの **`略称`** は当初の想定に無かったが、**入出庫の `設置先` と請求データの表記がこの略称**
  （例: `名取` `大昭和`）。`equipment_tenants` に `short_name` を追加する

**検証で見つかった実データの状態**（全期間830件で再検証。→ 8-4 に詳述）:

| 確認 | 結果 |
|---|---|
| 日付範囲 | **2019/02/25 〜 2026/08/10**（約7年半） |
| 区分×理由の内訳 | 入庫: 調達34・在庫調整30・繰延登録5／出庫: 共用部設置494・テナント設置262・新規入替4・**在庫調整1**（→ 8-4(6)） |
| 入出庫の請求先コードがテナントマスタに存在するか | **全件一致**（欠損なし） |
| 入出庫の `設置先` がテナントの `略称` と一致するか | **テナント設置は全件一致** |
| テナント設置の `受領タイムスタンプ` は作成時刻と別か | **全件別**（数分後）＝**署名を受け取った時刻として使える** |
| 入庫の `調達先` | **全件空**（実運用では使われていない） |
| 請求先コードが空のテナント | **2件あり**（どちらも退去済み）→ 主キー設計に影響（4-1） |
| 新規入替（4件）に請求先コード・設置階・設置先が入っている行 | 830件版では**1件あり**（入出荷ID 1122）だったが、**2026-08-12 受領の修正版CSVでは0件**（先方が消去済み）→ 8-4(1) |

- **過去の署名画像は移行しない**（CSV に載らない。現行 FileMaker を参照用に残す）。**2026-08-11 に「移行なしでよい」と確定**（確認事項⑤）
- 入出庫データは**全期間の再書き出しを受領済み**（2026-08-11。確認事項⑰）。830件で確定

### 8-2. 取込スクリプト

`scripts/import-equipment-csv.mjs`（ローカル実行。CI には載せない）

- Supabase の service role キーを環境変数で受け取り、`--dry-run` で**件数と差分だけ表示**してから本実行
- 突合キー: カテゴリ=`code` / 備品=`item_no` / テナント=`billing_code` / 入出庫=（日時 + 備品 + 数量 + 区分）
- **何度流しても二重登録にならない**こと（upsert）を必須要件にする
- 取込後の検証: **CSV の在庫数と、当システムが入出庫から算出した在庫数が全備品で一致すること**。
  ここがズレたら移行は失敗とみなす（データ移行の合否判定をこの1点に集約する）

### 8-3. 過去分をどこまで入れるか

現行画面の「3ヶ月前から表示」「年計」から、**年間の入出庫は多くても数百件**と見える。
全期間を入れても DB 的にまったく負担にならないため、**全件移行を推奨**（年計が過去年も正しく出る）。

> **【確定】全期間を移行する（2026-08-11。確認事項⑥）。** 「過去は必要に応じて全件移行可能」との回答による。
> なお**署名画像は CSV に含まれない**が、こちらも**移行なしで確定**している（確認事項⑤）。
> **全期間の再書き出しを受領済み**（確認事項⑰。2019/02/25〜2026/08/10・830件。8-1参照）。
> 想定どおりDBの負担にはならない件数だった。

### 8-4. 移行時に判断が要る点（実データの検証で判明。2026-08-11）

いずれも**取込スクリプトを書く前に決めておかないと、後から直すのが高くつく**もの。

#### (1)(2) 新規入替に請求先・設置先が入っている行 ← 【2026-08-12 解消】

3-4 で「新規入替に請求先（テナント）は持たせない」、5-4 で「新規入替は備考のみ」と決めたが、
**全期間データ（830件・新規入替は4件）で見ると、うち1件（入出荷ID 1122）だけ両方に値が入っていた**。

```
入出荷ID 1122 / 2026-02-21 / 新規入替 / 請求先コード 35000999（小泉産業 情報システム部）
  設置階 1 / 設置先「小泉情報」/ FLR40EX 1本 / 担当 廣畑
  備考「1階11-B号室　小泉関連会社入居準備」
```

> 興味深いことに、**同日・同じ備考の 1123 番が別行として存在し、そちらは請求先も設置階・設置先も
> 空**（備考のみ）。現行 FileMaker では「前日以前の入出庫は削除できない」（2-3）ため、**1122 番を
> 直接編集する代わりに、正しい内容で 1123 番を新たに起票した**（＝現場側で既に自己修正済み）と読める。
> 残る3件（969・1123・1163）はいずれも備考のみで、**今回の運用（新規入替は備考のみ）と完全に一致**。

**【回答・確定】**「除外して投入。またはテーブル制約は無くてよい」（確認事項⑬⑭）を受け、
移行方針は一度次のとおり確定していた。

- 新規入替の行は、**理由を問わず一律で請求先コード・設置階・設置先の値を取り込まず、備考のみ
  取り込む**方針とし、1122 番もこの規則に従わせる予定だった。

**その後、原因と修正版CSVが届いた（2026-08-12）。**「最初『テナント設置』として入力した後に
『新規入替』に変更した可能性がある」との説明とともに、**該当行の請求先・設置階・設置先を消去した
修正版CSV（`入出庫データ_20260812修正.csv`）**を受領し、機械検証した結果：

- 830件・日付範囲（2019/02/25〜2026/08/10）は元データと同一
- **1122番は `請求先=''／設置階=''／設置先=''` になり、1123番と完全に一致**（＝入力ミスがソース側で
  是正され、以後は起きない想定）
- 新規入替4件（969・1122・1123・1163）は**全件が備考のみ**の状態で揃った
- 8-4(6) の在庫調整1件（ID 474）は今回のクリーンアップ対象外のため**変化なし**（別件。8-4(6)参照）

**修正版CSVを移行の正とする。** これにより「取込時に請求先・設置階・設置先を強制的に空にする」
という**移行スクリプト側の特別処理は不要になった**（ソースが既にクリーンなため）。ただし、
以下の設計判断は将来の混入（記録ミスの再発）に備える防御として**そのまま維持する**:

- **DBの制約は持たない**（4-1）。`equipment_txn_tenant_only_tenant_reason` は**テーブル CHECK 制約としては
  持たず、アプリ側（`worker/lib/equipment.js` の保存時バリデーション）でのみ強制する**。
  理由は3つ: ①今回のような過去データの例外を DB が一律拒否すると移行のたびに手当てが要る、
  ②「テナント欄＝請求のトリガー」という業務ルールは今後も変わりうる柔軟さを残したい、
  ③実際に守るべきは「新規入替の**新規登録時**に請求先を選ばせない」という**画面の入力制御**であり、
  過去データの形まで DB が縛る必要はない
- 万一、将来また同様の入力ミス（テナント設置→新規入替への区分変更で欄が消えない）が別件で
  発生した場合に備え、**取込スクリプトは今後も一応「新規入替の請求先・設置階・設置先は取り込まない」
  防御的な処理を残す**（今回は結果的に不要だったが、無害かつ安全側）
- **新規入力**（今後）: **備考のみで確定**（確認事項⑱。5-4）。将来「やはり階・場所も分けて
  残したい」となっても、`floor`/`location` 列は共用部設置用に既に存在するため、画面だけ広げれば
  足り DB変更は不要

#### (3) 請求先コードが空のテナントが2件ある

```
5F 旧…株式会社 ケーディグラフィック（略称 ｹｰﾃﾞｨｸﾞﾗﾌｨｯｸ）退去済み
7F 日本機械商事 株式会社（略称 日本機械）退去済み
```

`billing_code` を主キーにする設計（4-1）だと**この2件が入らない**。どちらも退去済みで、
入出庫データからも参照されていない。

- **対処**: 主キーを `billing_code` から**サロゲートキー（uuid）**に変え、`billing_code` は
  「NULL 可・NULL でなければ一意」にする。入出庫からの参照も uuid にする。
  こうしておくと、将来また請求先コード未設定のテナントが来ても破綻しない
- 取り込み自体は行う（過去の記録がこのテナントを指す可能性を残すため）

#### (4) 署名は「時刻だけ」移行できる

テナント設置**262件**の `受領タイムスタンプ` は作成時刻と**全件別**（数分後）だった。
＝ FileMaker 上でも実際に署名をもらった時刻が入っている。したがって:

- `signed_at` には**受領タイムスタンプをそのまま移行する**（画像は移行しない）
- これにより移行分は「**署名済みだが画像は無い**」状態になる。5-5 の「署名待ち」バッジは
  `signed_at` が空のものだけに出るので、**過去分が誤って「署名待ち」に出ることはない**
- 画面では、`signed_at` あり・`signature_key` なし・移行データの3条件で
  「**署名あり（画像は現行 FileMaker を参照）**」と表示する
- 移行元の `入出荷ID` を `legacy_txn_id` として保持する（再取込の冪等性・問い合わせ時の突合のため）

#### (5) 使われていない項目

`調達先` は入庫**34件すべて空**だった。項目としては残すが、**画面では折りたたむ等、優先度を下げてよい**。

#### (6) 「区分=出庫・理由=在庫調整」の行が1件だけある

現行画面の注記は「在庫調整は入庫から」（2-1）で、4-1 の DB 制約もこれに合わせて
`reason='adjust'` は `kind='in'` のときだけ許可している。だが全期間データに**1件だけ例外**があった。

```
入出荷ID 474 / 2021-07-12 / 出庫 / 在庫調整 / 請求先コード 73070011（大昭和）
  設置階 2 / 設置先「大昭和」/ FLR40SW 4本（出庫数量欄に4、入出庫数量は -4）
```

テナントの請求情報も付いており、**元はテナント設置として登録した後、数量の誤りに気づいて
理由だけ「在庫調整」に直した**行と見える（在庫を4本分マイナス補正）。件数は830件中1件のみで、
運用の主流ではなく単発の訂正と判断できる。

- **移行**: `出庫4・在庫調整` は**数量的に等価な `入庫-4・在庫調整` に正規化して取り込む**
  （`入出庫数量=-4` は元データにも記録済みなので、変換は機械的にできる）。
  これで現行 DB 制約（在庫調整は入庫のみ）を変更せずに済み、今後の運用（「在庫調整は入庫から」）とも一致する
- テナント情報（請求先・設置階・設置先）は在庫調整には持たせない方針（4-1）のため**取り込まない**。
  ただし何のための調整だったか分かるよう、**備考へ「（移行時に出庫記録から統合。元テナント: 大昭和）」等の注記を自動で足す**

#### (7) 数量0の出庫記録が1件ある ←【2026-08-12 実装時の取込検証で新規発見】

```
入出荷ID 726 / 2023-01-31 / 出庫 / 共用部設置 / FLR40SW 0本
  備考「旧電灯の余りを使用したため、０本扱い。」
```

備考のとおり、在庫を持たない廃材を使い回した設置作業の記録であり、**在庫への影響が無い**。
4-1 の DB 制約（`quantity <> 0`）にそのまま反する1件だが、在庫計算上は無くても支障がない。

- **移行**: **取り込まない**（在庫影響ゼロのため除外しても実績としての欠落は生じない）。
  必要であれば作業記録として備考欄付きで別途参照できるよう、取込ログに1行残すだけにする

---

### 8-5. 各種出力（帳票）— 2026-08-11 に実物を受領し要件確定

当初「どの帳票が要るか未確認」として**範囲外**にしていた（1章・確認事項⑪）が、実物を2点いただき
仕様が判明したため、**範囲に含める**。**この2つが「署名を取る目的」そのもの**なので、
署名機能（5-5）とセットで設計する。

#### 8-5-1. 修理伝票（印刷用 PDF）— 署名の出口

いただいた `修理伝票_印刷用.png` の再現。**テナント設置1件につき1枚**、**A4縦に2枚（2-up）**。

```
                    修 理 伝 票
 取替日: 令和8年7月22日                    備後町コイズミビル
 ┌──────┬────────────────────────┬──────────┐
 │  階  │        テナント名          │  受領印   │
 ├──────┼────────────────────────┼──────────┤
 │  7   │      （有）栄和 様          │ 〔署名画像〕│
 └──────┴────────────────────────┴──────────┘
   ◆ランプ取替
     ┌────────────────────────┬────────┐
     │          型番              │  本数  │
     ├────────────────────────┼────────┤
     │  FLR40SN (メロウホワイト)    │   2    │
     └────────────────────────┴────────┘
                                        ┌────────┐
                                        │ 担当者 │
                                        ├────────┤
                                        │  西川  │
                                        └────────┘
```

| 欄 | データの出どころ |
|---|---|
| 取替日 | `occurred_at` を**和暦**で（「令和8年7月22日」）。※和暦変換が要る点に注意 |
| 建物名（右上） | 固定文字列「備後町コイズミビル」（**2026-08-11 確定**。確認事項⑮） |
| 階 | テナントの `floor` |
| テナント名 | テナントの**正式名称**＋「様」（略称ではない。例「合同会社備後町富士法律経済研究所 様」） |
| 受領印 | **署名画像**（`signature_key`）。未署名なら空欄のまま印刷し、紙にもらう運用もできる |
| 型番 | 備品の**「備品名 (補足)」表記**（`FLR40SN (メロウホワイト)`。CSV の `備品名_補足` と同じ組み立て） |
| 本数 | `quantity` |
| 担当者 | `staff_name` |

- **実装は自主検査表・残留塩素の帳票PDFと同じ型**（画面外にA4のシートを組んで html2canvas → jsPDF →
  短時間トークンでアプリ内プレビュー。設計書 4-12・4-14 と `print-and-pdf-download` スキル）
- **2-up のため、1ページに2件を積む**。件数が奇数なら下半分は空белのまま
- 出力単位は「期間を指定して、その期間のテナント設置を全部まとめて1つのPDF」を既定にする
  （月末にまとめて印刷する運用を想定）。1件だけ出したい場合に備え、履歴の行からの単票出力も付ける
- **owner（小泉産業様）にも出す**（2026-08-11 確定。9章）。現在の運用でも修理伝票を小泉産業様へ
  提供しているため、これを踏襲する

#### 8-5-2. 修理データ（請求連携用の表）— 6-2 API と同じ中身

いただいた `修理データ_2026年 7月分.xlsx` は、実質 CSV 相当の1シート。**テナント・備品ごとの月次集計**。

| 列 | 内容 | 実例 |
|---|---|---|
| 修理年月 | 対象月の1日（日付値） | `2026-06-01` |
| 請求先コード | テナントの請求先コード | `73060011` |
| 略称 | テナントの**略称** | `名取` |
| 備品ID | FileMaker の備品ID | `2` |
| 備品名 | 備品名（補足なし） | `FLR40SW` |
| c_出庫数量合計 | その月・そのテナント・その備品の**合計本数** | `2` |
| c_設置日付列挙_短縮 | 設置日と型番の列挙 | `6/15 FLR40SW` |

**検証の結果、次が確定した**（受領した入出庫データと突き合わせて完全一致）:

- ファイル名は「2026年**7月**分」だが、中身の修理年月は **2026-06-01**。
  → **請求月の前月が実績月**（6月の設置を7月に請求する）。API の `month` の意味を取り違えないこと
- 2026年6月の出庫は10件あったが、この表に載ったのは**テナント設置の3件だけ**。
  **共用部設置・新規入替・在庫調整・調達は載っていない** → 3-4 の「請求対象はテナント設置のみ」を
  実データが裏づけた
- 同一テナント・同一備品が月内に複数回あれば**合算**し、日付は列挙する形（1行にまとめる）

**この表は 6-2 の公開 API と同じ内容**なので、次の2経路で出せるようにする。

1. **画面からのダウンロード**（CSV）… 現行の運用（Excel を落として請求システムへ渡す）をそのまま置き換える
2. **6-2 の API**（JSON）… FileMaker 側から直接引く。将来こちらへ寄せる

> 6-2 は当初「1件ずつの明細を返す」形だったが、**実際の用途は月次集計**だと判明した。
> 明細（`installations`）に加えて**集計（`billing`）も返す**ようにし、現行の表をそのまま作れるようにする。

---

## 9. 権限・修正ロック

現行画面に明記されている一般ユーザ制約を、**サーバー側で**実装する（画面側の制御は補助）。

| 操作 | staff | admin | owner（小泉産業様） |
|---|---|---|---|
| 閲覧 | ○ | ○ | **○（2026-08-11 変更。確認事項⑦）** |
| 入庫の登録 | ○ | ○ | × |
| **入庫の修正** | **×**（登録後は不可） | ○ | × |
| 出庫の登録 | ○ | ○ | × |
| **署名済み出庫の修正** | **×** | ○ | × |
| 未署名の出庫の修正 | ○ | ○ | × |
| **前日以前のレコードの削除** | **×** | ○ | × |
| 当日登録分の削除 | ○ | ○ | × |
| マスタ（備品・カテゴリ）の編集 | ○ | ○ | × |
| 修理伝票PDF（署名画像を含む）の出力 | ○ | ○ | **○（2026-08-11 確定。下記）** |
| 修理データ（請求連携用の表）の出力 | ○ | ○ | ○ |

> **owner にも備品セクションを見せる（2026-08-11 確定。確認事項⑦）。** 当初は「見せない」前提だったので、
> 日報と同じ**閲覧のみ**（書き込みは全てサーバー側で403）で開放する。
>
> **修理伝票PDF（署名画像入り）も owner に出す（2026-08-11 確定。確認事項⑯）。** 当方は「他社の
> 受領印にあたるため社内に留めるべき」と考え一度は非公開を提案したが、「**現在の運用でも修理伝票の
> ファイルを小泉産業様へ提供している**。実態として今後も直接閲覧してもらって差し支えない」との
> ご回答により訂正した。⑦の「見せる」はアクセス権としては制限不要という意味だった。
> **現行の運用をそのまま踏襲する**形になるため、Phase 3.5（8-5）での追加実装は不要（アクセス制御を
> 付けない＝閲覧権限があれば見える、という素直な実装でよい）

- 判定は `worker/lib/equipment.js` の共通関数に閉じ込める（`canEditTransaction(auth, txn)`）
- 拒否時は 403 と**理由の分かるメッセージ**を返す（「受領サイン済みのため修正できません。管理者に依頼してください」）
- **署名は後から付く前提**（5-5。確認事項④）。したがって上表の「署名済み／未署名」は
  レコード作成時ではなく**その時点の `signed_at` の有無**で判定する。未署名の間は staff も修正でき、
  **署名を足した瞬間からロックがかかる**。なお「署名を足す」操作自体は、未署名レコードに対して
  staff にも許可する（そうしないと後日もらう運用が回らない）
- 現行の「修正ロック」チェックボックスは、**誤操作防止のための画面側トグル**として同等機能を用意する
  （既定オン。オフにしないと一覧から編集モーダルを開けない）。**2026-08-11 に実施決定**（確認事項⑨）

---

## 10. 非機能

| 観点 | 方針 |
|---|---|
| セキュリティ | 6-2 の公開 API 以外は既存どおり JWT。公開 API は APIキー＋IP制限＋レート制限＋読み取り専用。設計書8章に追記 |
| ストレージ | 署名 PNG は 1枚 10〜30KB。年間数百枚でも数MB で、無料枠 1GB にほぼ影響しない。従量課金画面の使用量表示にはそのまま合算される |
| AI コスト | **この機能では Claude を使わない**（増加ゼロ）。将来、伝票の写真読み取り等を足すなら別途 |
| PWA | 変更なし。SW には手を入れない（fetch ハンドラを足さない＝既存の鉄則） |
| CSP | 変更なし。署名は canvas → Blob で完結し、外部への接続を増やさない。FileMaker への接続は**すべて Worker 側**で行うためブラウザの `connect-src 'self'` に触れない |
| 通知 | **在庫が警告数量を下回ったら既存の Web Push で知らせる（2026-08-11 実施決定。確認事項⑩）**。しきい値は備品マスタの `warn_qty`（警告数量）を使う＝備品ごとに設定でき、既存項目なので追加のマスタ変更は不要。詳細は下記 |

### 10-1. 在庫警告の Web Push（2026-08-11 実施決定）

既存の通知基盤（`worker/lib/push.js` / `push_subscriptions`。設計書 4-9章）をそのまま使う。

- **しきい値**: 備品マスタの `warn_qty`。**備品ごとに設定**でき、現行 FileMaker の「警告数量」と同じ意味。
  未設定（`null`）の備品は通知しない
- **判定のタイミング**: 出庫を登録・修正した直後に、その備品の現在庫を再計算して
  `現在庫 <= warn_qty` なら通知する。定期バッチではなく**在庫が動いた時だけ**見るので無駄がない
- **鳴らしすぎない工夫**（ここが実装の肝）: 素直に作ると、警告数量を下回った状態で出庫するたびに
  毎回鳴ってしまう。**「下回っていない → 下回った」に変化した時だけ**通知する（またいだ瞬間だけ）。
  そのため備品ごとに最後に通知した状態を持たせる（`equipment_items.warned_at`。在庫が
  `warn_qty` を上回ったらクリア＝入庫で解消したら次回また鳴らせる）
- **本文**: 「FLR40SW (白色) の在庫が 3 になりました（警告数量 5）。発注をご検討ください。」のように
  **備品名・現在庫・警告数量**を入れる。タップで在庫一覧（`/equipment`）を開く
- 通知先は既存と同じく**購読済みの全員**（担当者を絞らない）。発注は誰が気づいてもよいため
- 送信失敗（購読失効）の扱い・iOS はホーム画面 PWA のみ受信可という制約も既存と同じ

---

## 11. 開発フェーズと見積り

各フェーズは独立してリリースでき、**現行 FileMaker と並行運用しながら**進める（日報と同じ進め方）。

| Phase | 内容 | 前提 | 目安 |
|---|---|---|---|
| **0. 準備** | 12章の確認事項の回答、CSV 4本の受領 | — | **①〜⑱すべて2026-08-12までに回答済み**（CSV・帳票の実物も受領・検証済み、新規入替の修正版CSVも受領済み） |
| **1. 基盤＋在庫と入出庫（共用部まで）** | DB・ビュー / ヘッダに「備品」追加 / 在庫一覧 / 履歴 / 入庫モーダル / 出庫（共用部・不良品処分） / 備品・カテゴリマスタ / CSV 取込スクリプト | Phase 0 の CSV | 1〜2セッション |
| **2. テナント設置＋署名** | テナント一覧（CSV 取込分） / 出庫（テナント設置・新規入替） / **署名キャプチャ** / 署名の表示・保管 / **署名の後付け（後日もらう運用）と「署名待ち」の可視化** | Phase 1 | 1〜2セッション |
| **3. 権限・修正ロック・仕上げ** | 9章の権限判定 / 修正ロック / 在庫警告表示 / **在庫警告の Web Push（10-1）** / 実機フィードバック対応 | Phase 2 | 1セッション |
| **3.5 帳票（各種出力）** | **修理伝票PDF（署名入り・A4に2枚）** / **修理データのCSV出力（月次集計）**（8-5） | Phase 2（署名） | 1セッション |
| **4. 設置実績の提供 API** | 6-2 の公開 API（明細＋月次集計） / APIキー発行 / FileMaker 側からの疎通確認 | Phase 2 | 0.5〜1セッション |
| **5. テナントマスタ同期** | 7-1 の受信用エンドポイント（Push・全件洗い替え） / 保存時トリガースクリプトの提示 | **FileMaker 側の準備（7-1 の表 1〜2）** | 1セッション＋先方作業 |
| **6. 並行運用と切替判断** | 実運用での突き合わせ、現行 FileMaker アプリの停止判断 | 全 Phase | 運用期間 |

- **Phase 3.5（帳票）は、現行運用を置き換えるうえで実質必須。** 修理伝票は署名の出口であり、
  修理データは請求へ回している実データなので、この2つが出せて初めて現行 FileMaker を止められる
- **Phase 4 は Phase 5 を待たない。** 当システム側だけで完結するので、テナントマスタの同期が
  整う前でも FileMaker 側から実績を引けるようにできる。**7-1 も Push 化（2026-08-12）したことで
  Phase 4・5 とも「Worker 側の受信口を先に作っておき、FileMaker 側の送信スクリプトは後から
  つなぎ込む」という同じ進め方ができるようになった**（旧案では 7-1 だけ Worker → FileMaker への
  到達性整備という別種の前提が要った）
- Phase 1・2 の間はテナントマスタが**CSV 取込の静的コピー**になる。新規入居・退去は
  テナント一覧画面から手で直せるようにしておき（`source='manual'`）、Phase 5 で自動化する
- 「1セッション」= これまでの日報の各 Phase と同程度の作業量（実装＋ローカル検証＋文書更新）。
  **実機確認とフィードバック対応の往復は別途**（日報では毎回2〜3ラウンド発生している）

---

## 12. 確認事項

### 12-1. 回答済み（2026-08-11〜08-12）

| # | 内容 | **回答** | 設計への反映 |
|---|---|---|---|
| ① | FileMaker Server の外部到達性（7-1 の A/B/C） | **オンプレで技術的に可能なことは全て設定可能。バージョンは最新 2026**（後日「Push の方が良いかもしれない」との追加意見あり） | 当初は**方式A（Cloudflare Tunnel）**で確定していたが、**2026-08-12、7-1 全体を Push 方式に変更**したため到達性の論点自体が解消。FileMaker Server を外部公開する必要も Tunnel も不要になった（→ 7-1） |
| ② | 「新規入替」「繰延登録」の入力項目 | **新規入替の入力は備考のみ。繰延は通常の入荷と同じ項目が使えればOK**（移行時の入力で使う） | **新規入替＝共通項目（備品・数量・担当者）＋備考のみ**に変更（当初の仮置き「テナント設置と同じ項目」は誤りだったため訂正）。**繰延登録＝調達と同じ項目**（仮置きどおり）（→ 5-4） |
| ③ | 「新規入替」に設置先（テナント／共用部）の入力は要るか | **設置先は備考欄に書く。テナント名を入れるとそこへの請求を意味する。新規入替は請求対象外** | **極めて重要**。テナント欄は単なる場所のラベルではなく**請求のトリガー**だと判明した。`tenant_code` を持てるのはテナント設置だけに限定し、画面・DB制約・外部APIの全レイヤーで縛る（→ 3-4・4-1・5-4・6-2） |
| ④ | テナント設置で署名は必須か | **署名は必ずもらえるが、システム上は必須にしなくてよい。先に入力しておいて後から署名だけもらうこともある** | **必須をやめ、「あとから署名だけ足せる」導線を設計に追加**。保存とサインの同時送信を前提にしていた作りを改める（→ 5-5・9章）。これは単なる必須解除ではなく**運用フローの追加**なので影響が大きい |
| ⑤ | 過去の署名画像を移行するか | **移行なしでよい** | 移行しない（過去分は現行 FileMaker を参照用に残す）（→ 8-3） |
| ⑥ | 入出庫の過去データの移行範囲 | **必要に応じて全件移行可能** | **全期間移行で確定**（→ 8-3） |
| ⑧ | 担当者の候補の出どころ | **過去の設置担当の Distinct** | 仮置きどおり。過去の入出庫の `staff_name` から重複を除いて候補にする（→ 5-8） |
| ⑨ | 「修正ロック」を残すか | **行う** | 現行どおり実装する（既定オン）（→ 9章） |
| ⑩ | 在庫警告の Web Push 通知 | **行う。警告発出の本数は備品マスタに設定** | 実装する。しきい値は備品マスタの既存項目 `warn_qty`（警告数量）をそのまま使う（→ 4-1・5-6・10章・11章 Phase 3） |
| ⑦ | owner（小泉産業様）に備品セクションを見せるか | **見せる。修理伝票PDF（署名入り）も含めて閲覧させて差し支えない**（現行運用でも提供している） | 日報と同じ**閲覧のみ**で開放。修理伝票PDFも対象に含める（→ 9章・8-5-1） |
| ⑪ | 「各種出力」で現在どんな帳票を出しているか | **実物2点を受領**（修理伝票の印刷イメージ・修理データのExcel） | **範囲に含める**。修理伝票PDF（署名の出口）と修理データ（請求連携用の月次集計）として仕様化（→ 8-5）。6-2 の API も月次集計を返す形に見直し |
| ⑫ | CSV の列名・件数 | **4本を受領**（カテゴリ7 / 備品22 / テナント52 / 入出庫データ） | 全件を機械検証し、実際の列・使う列・捨てる列を確定（→ 8-1） |
| ⑬⑭ | 新規入替に請求先・設置先が入っている過去データの扱い（実データ検証で1件発見） | **除外して投入。または、入力チェックとしては必要だがテーブル制約は無くてよい**（→ **2026-08-12 追記**: 「テナント設置として入力後に新規入替へ変更した際の消し忘れの可能性がある。該当行を消去した修正版CSVを再添付する」と説明があり、修正版を受領） | **移行**: 2026-08-12 受領の修正版CSVでは該当行が既にクリーンなため、除外処理は不要になった（8-4(1)(2)）。**設計**: 将来の同種の入力ミスに備え、取込スクリプトの防御的な除外処理と、DBのCHECK制約を持たずアプリ層バリデーション（`worker/lib/equipment.js`）でのみ強制する方針はそのまま維持（→ 3-4・4-1・8-4(1)(2)） |
| ⑮ | 修理伝票の右上「備後町コイズミビル」は固定でよいか | **固定で良い** | 固定文字列で確定（→ 8-5-1） |
| ⑯ | 修理伝票PDF（署名画像入り）を owner に見せるか | **現在の運用でも修理伝票ファイルを提供しており、直接閲覧させて差し支えない。⑦の回答はアクセス権として禁止する必要はないという意味だった** | 当方は一度「社内限定」を提案したが撤回。**owner にも公開**（→ 9章・8-5-1） |
| ⑰ | 入出庫データの全期間の再書き出し | **再添付済み** | **受領・検証済み**（2019/02/25〜2026/08/10・830件。→ 8-1・8-3） |
| ⑱ | 新規入替は今後の入力も「備考のみ」で確定したが、実データでは過去に階・設置先も使われていた（8-4(1)(2)）。今後、「どこに入れ替えたか」を集計できる形（階・場所欄）で残したい場面はあるか | **備考だけで十分** | **確定**。新規入替の入力欄は共通項目＋備考のみのまま（5-4）。将来「やはり分けて残したい」となった場合も、`floor`/`location` 列は共用部設置用に既に存在するため、**画面だけ広げれば足り DB変更は不要** |

**確認事項18件、すべて回答をいただき反映済み。** 実装着手の前提が揃った状態。

---

## 13. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| **テナント全件洗い替え同期の誤爆**（7-1・6-3） | 不完全な配列（送信失敗・スクリプト不具合で空や一部欠けたもの）が届くと、有効なテナントを一括で誤って論理削除（`moved_out=true`）してしまう | Worker 側で空配列・極端な件数減少を拒否するガードを必須にする（6-3）。2026-08-12、Pull→Push・全件洗い替え方式に確定したことで旧リスク「FileMaker Server の常時公開」は解消 |
| **署名が iOS 実機で書けない／ずれる** | この機能の中心が使えない | Pointer Events・`touch-action: none`・DPR 対応を最初から入れる。**iPad と iPhone の実機確認を Phase 2 の完了条件にする**。過去、モーダル・写真は Chromium で再現しない iOS 固有の不具合を複数回踏んでいる |
| **在庫数が現行と合わない** | 移行の信頼性が崩れる | 8-2 の検証（全備品で在庫一致）を移行の合否判定にする。合わない場合は原因（重複行・区分の取り違え）を特定してから本番投入 |
| **公開 API のキー漏洩** | 設置実績が第三者に読まれる | IP 制限・レート制限・読み取り専用・ログ記録。ローテーション手順を HANDOFF に明記 |
| **ヘッダが3セグメントで狭幅に収まらない** | iPhone でヘッダが崩れる | Phase 1 の最初に 375px 幅で確認する。収まらなければトークンで調整（ラベルの短縮より先にパディング） |
| **テナントの改称・退去で過去の記録が読めなくなる** | 帳票・実績 API の整合が崩れる | `tenant_name` を記録時点のスナップショットとして持ち、テナント行は物理削除せず `moved_out` を立てるだけにする（7-1 の全件洗い替え同期でも同様） |
| **入出庫の一覧が将来重くなる** | 表示が遅くなる | 既定を「3ヶ月」に絞り、在庫・年計はビューで集計。行数が万件規模になったら残高計算を SQL 側（窓関数の RPC）へ移す |

---

## 14. 実装時に触るファイル（見取り図）

| 追加 / 変更 | ファイル |
|---|---|
| 追加 | `supabase/schema.sql`（4章のテーブル・ビュー）＋同内容のマイグレーション |
| 追加 | `worker/lib/equipment.js`（社内 API・6-2 の公開API・7-1 のテナント同期受信口を含む。Phase 1〜5） |
| 変更 | `worker/index.js`（`/api/equipment/*` の分岐）、`worker/lib/storage.js`（プレフィクス運用のみ。改修不要の見込み） |
| 追加 | `src/pages/Equipment.jsx`（在庫一覧）/ `EquipmentHistory.jsx` / `EquipmentItems.jsx` / `EquipmentTenants.jsx` |
| 追加 | `src/components/EquipmentTxnForm.jsx`（入出庫モーダル）/ `SignaturePad.jsx`（署名） |
| 追加 | `src/lib/equipment.js`（API クライアント） |
| 変更 | `src/App.jsx`（ルート）、`src/components/AppHeader.jsx`（3セグメント・メニュー） |
| 変更 | `src/styles/ui.css`（共通部品として足すものがあれば。個別 CSS は最後の手段） |
| 追加 | `scripts/import-equipment-csv.mjs`（初期データ取込） |
| 変更 | `docs/task-management-spec-cloudflare.md`（4-15章・5章・6章・8章）、`docs/HANDOFF.md`、この文書 |
